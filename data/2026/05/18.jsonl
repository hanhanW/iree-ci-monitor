{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:30:35Z", "conclusion": "success", "created_at": "2026-05-18T06:30:24Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470402410, "labels": ["ubuntu-24.04"], "name": "setup_metadata", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478355", "started_at": "2026-05-18T06:30:26Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:43:13Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426291, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478357", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426299, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478359", "started_at": "2026-05-18T06:30:37Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426302, "labels": ["macos-14"], "name": "macos :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478366", "started_at": "2026-05-18T06:30:38Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:49:58Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426305, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478362", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426307, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478358", "started_at": "2026-05-18T06:30:37Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426312, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478360", "started_at": "2026-05-18T06:30:37Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:46:55Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426313, "labels": ["windows-2022"], "name": "windows :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478364", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:38:57Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426317, "labels": ["macos-14"], "name": "macos :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478365", "started_at": "2026-05-18T06:30:39Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426320, "labels": ["windows-2022"], "name": "windows :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478363", "started_at": "2026-05-18T06:30:37Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:32:40Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426323, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-tf-compiler-tools-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478361", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T06:58:23Z", "conclusion": "failure", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426324, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478356", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T06:30:24Z", "conclusion": "success", "created_at": "2026-05-18T06:29:40Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76470307584, "labels": ["ubuntu-24.04"], "name": "Tag candidate release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017347263", "run_id": 26017347263, "runner_name": "GitHub Actions 1000478354", "started_at": "2026-05-18T06:29:42Z", "status": "completed", "workflow_id": 17505351, "workflow_name": "Schedule candidate release", "workflow_path": ".github/workflows/schedule_candidate_release.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T05:42:44Z", "conclusion": "failure", "created_at": "2026-05-18T05:33:22Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76464514225, "labels": ["ubuntu-24.04"], "name": "colab", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26015470241", "run_id": 26015470241, "runner_name": "GitHub Actions 1000478350", "started_at": "2026-05-18T05:33:24Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T05:37:33Z", "conclusion": "success", "created_at": "2026-05-18T05:33:22Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76464514259, "labels": ["ubuntu-24.04"], "name": "samples", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26015470241", "run_id": 26015470241, "runner_name": "GitHub Actions 1000478351", "started_at": "2026-05-18T05:33:23Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-05-18T07:45:46.994006Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T05:42:51Z", "conclusion": "failure", "created_at": "2026-05-18T05:42:44Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76465397516, "labels": ["ubuntu-24.04"], "name": "samples_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26015470241", "run_id": 26015470241, "runner_name": "GitHub Actions 1000478352", "started_at": "2026-05-18T05:42:46Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "success", "created_at": "2026-05-18T13:25:28Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535525707, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478469", "started_at": "2026-05-18T13:25:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535561822, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:38Z", "conclusion": "success", "created_at": "2026-05-18T13:25:28Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535525786, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478468", "started_at": "2026-05-18T13:25:30Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:30:28Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559600, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478474", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:26:42Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559606, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478476", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559679, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478477", "started_at": "2026-05-18T13:25:42Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:34Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559703, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478479", "started_at": "2026-05-18T13:25:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:00Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559709, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478475", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:26Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559715, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478481", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559727, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478478", "started_at": "2026-05-18T13:25:43Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:28:17Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559741, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478484", "started_at": "2026-05-18T13:25:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:46Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559768, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478473", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:04Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559773, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478485", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559820, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:16Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559940, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478480", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559999, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560016, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:28:10Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560024, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478483", "started_at": "2026-05-18T13:25:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:37:11Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560090, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478482", "started_at": "2026-05-18T13:25:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560099, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560285, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "", "started_at": "2026-05-18T13:25:39Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560323, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-windows-scale-4lxzk-runner-kk7tf", "started_at": "2026-05-18T13:29:56Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:28:27Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560402, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478486", "started_at": "2026-05-18T13:26:22Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560560, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560607, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560733, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560741, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560874, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:25:39Z", "conclusion": "skipped", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535561068, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": null, "started_at": "2026-05-18T13:25:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:26:19Z", "conclusion": "success", "created_at": "2026-05-18T13:25:28Z", "event": "dynamic", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535526524, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036366470", "run_id": 26036366470, "runner_name": "GitHub Actions 1000478470", "started_at": "2026-05-18T13:25:31Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:27:15Z", "conclusion": "success", "created_at": "2026-05-18T13:25:28Z", "event": "dynamic", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535526557, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036366470", "run_id": 26036366470, "runner_name": "GitHub Actions 1000478472", "started_at": "2026-05-18T13:25:31Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:26:47Z", "conclusion": "success", "created_at": "2026-05-18T13:25:28Z", "event": "dynamic", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535526575, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036366470", "run_id": 26036366470, "runner_name": "GitHub Actions 1000478471", "started_at": "2026-05-18T13:25:31Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:42Z", "conclusion": "success", "created_at": "2026-05-18T13:04:28Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531616036, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478452", "started_at": "2026-05-18T13:04:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:42Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531659482, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "", "started_at": "2026-05-18T13:04:42Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "success", "created_at": "2026-05-18T13:04:27Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531615394, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478451", "started_at": "2026-05-18T13:04:31Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:05:44Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655263, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478462", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:15:34Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655266, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478453", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:07:22Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655293, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478458", "started_at": "2026-05-18T13:04:45Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:46Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655343, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478460", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:09:24Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655383, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478463", "started_at": "2026-05-18T13:04:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:14Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655449, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478457", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:21Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655491, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478456", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:12Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655534, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478461", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655549, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-fgnws", "started_at": "2026-05-18T13:31:33Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:15:24Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655557, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478455", "started_at": "2026-05-18T13:04:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:17Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655576, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478459", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655585, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-windows-scale-4lxzk-runner-jcz2f", "started_at": "2026-05-18T13:06:01Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655603, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-nc8tc", "started_at": "2026-05-18T13:32:02Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:38Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655620, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478464", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:07:05Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655676, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478465", "started_at": "2026-05-18T13:04:45Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:27Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655679, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478466", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655681, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "", "started_at": "2026-05-18T13:04:41Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:16:13Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655711, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478454", "started_at": "2026-05-18T13:04:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655753, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "", "started_at": "2026-05-18T13:04:41Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656084, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656116, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656240, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656380, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656517, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656550, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:04:41Z", "conclusion": "skipped", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531656637, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": null, "started_at": "2026-05-18T13:04:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:06:46Z", "conclusion": "success", "created_at": "2026-05-18T13:04:27Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531615077, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297014", "run_id": 26035297014, "runner_name": "GitHub Actions 1000478450", "started_at": "2026-05-18T13:04:31Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:09:03Z", "conclusion": "success", "created_at": "2026-05-18T13:04:27Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531615080, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296941", "run_id": 26035296941, "runner_name": "GitHub Actions 1000478449", "started_at": "2026-05-18T13:04:30Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:53:34Z", "conclusion": "success", "created_at": "2026-05-18T12:52:02Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529382588, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656920", "run_id": 26034656920, "runner_name": "GitHub Actions 1000478430", "started_at": "2026-05-18T12:52:05Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:56:02Z", "conclusion": "success", "created_at": "2026-05-18T12:52:02Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529383003, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656881", "run_id": 26034656881, "runner_name": "GitHub Actions 1000478433", "started_at": "2026-05-18T12:52:04Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "success", "created_at": "2026-05-18T12:52:02Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529382932, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478432", "started_at": "2026-05-18T12:52:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:53:40Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451174, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478437", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:56:59Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451219, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478439", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:30Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451226, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478436", "started_at": "2026-05-18T12:52:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:12Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451243, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478440", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:53:54Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451262, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478438", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:10Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451267, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478445", "started_at": "2026-05-18T12:52:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:03:32Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451270, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478441", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:04:03Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451282, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478442", "started_at": "2026-05-18T12:52:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:53:56Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451298, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478444", "started_at": "2026-05-18T12:52:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:30Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451315, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478443", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:12Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451336, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478435", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451346, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-dnjxd", "started_at": "2026-05-18T13:30:05Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:13Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451359, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478434", "started_at": "2026-05-18T12:52:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:03:54Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451381, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478447", "started_at": "2026-05-18T12:52:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:28:34Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451426, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-windows-scale-4lxzk-runner-sppjc", "started_at": "2026-05-18T12:52:27Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451431, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-9rr72", "started_at": "2026-05-18T13:30:48Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:54:22Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451473, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478446", "started_at": "2026-05-18T12:52:31Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451491, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-j8jqj", "started_at": "2026-05-18T13:31:18Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451600, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-r2ts4", "started_at": "2026-05-18T13:31:23Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451677, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:25Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451895, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451941, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451971, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529452023, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529452376, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:25Z", "conclusion": "skipped", "created_at": "2026-05-18T12:52:26Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529452396, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": null, "started_at": "2026-05-18T12:52:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T12:52:16Z", "conclusion": "success", "created_at": "2026-05-18T12:52:02Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529383058, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478431", "started_at": "2026-05-18T12:52:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:34:31Z", "conclusion": "success", "created_at": "2026-05-18T12:52:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529426060, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "azure-linux-scale-rfhjf-runner-8wf24", "started_at": "2026-05-18T13:26:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:36:00Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292361, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478493", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:36:34Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292375, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark01-ci", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292388, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:36:42Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292426, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478492", "started_at": "2026-05-18T13:34:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292453, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-mzws8", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292457, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:31Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292534, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:31Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292552, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:31Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292589, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark01-ci", "started_at": "2026-05-18T13:36:37Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292611, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292629, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark75-ci", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292654, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:35:30Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292683, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478499", "started_at": "2026-05-18T13:34:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292684, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292690, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478494", "started_at": "2026-05-18T13:34:35Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292692, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478497", "started_at": "2026-05-18T13:34:35Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292711, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478496", "started_at": "2026-05-18T13:34:35Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292715, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478495", "started_at": "2026-05-18T13:34:36Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292718, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292721, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292731, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark10-ci", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292736, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292738, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292768, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292815, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292836, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-xfgnv", "started_at": "2026-05-18T13:35:55Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292872, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark55-ci", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292967, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": null, "started_at": "2026-05-18T13:34:32Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537293044, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478498", "started_at": "2026-05-18T13:34:37Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537293092, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478500", "started_at": "2026-05-18T13:34:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:05:15Z", "conclusion": "failure", "created_at": "2026-05-18T11:04:35Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511711145, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029577124", "run_id": 26029577124, "runner_name": "GitHub Actions 1000478414", "started_at": "2026-05-18T11:04:38Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:05:07Z", "conclusion": "failure", "created_at": "2026-05-18T11:04:35Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511711165, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029577124", "run_id": 26029577124, "runner_name": "GitHub Actions 1000478415", "started_at": "2026-05-18T11:04:37Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:06:12Z", "conclusion": "success", "created_at": "2026-05-18T11:04:35Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511711334, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029577124", "run_id": 26029577124, "runner_name": "GitHub Actions 1000478416", "started_at": "2026-05-18T11:04:38Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:04:47Z", "conclusion": "success", "created_at": "2026-05-18T11:04:34Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511709088, "labels": ["ubuntu-latest"], "name": "build", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029576112", "run_id": 26029576112, "runner_name": "GitHub Actions 1000478413", "started_at": "2026-05-18T11:04:38Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:04:55Z", "conclusion": "success", "created_at": "2026-05-18T11:04:48Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511744343, "labels": ["ubuntu-latest"], "name": "report-build-status", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029576112", "run_id": 26029576112, "runner_name": "GitHub Actions 1000478417", "started_at": "2026-05-18T11:04:51Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "Deployed 3f160df with MkDocs version: 1.6.1", "completed_at": "2026-05-18T11:05:00Z", "conclusion": "success", "created_at": "2026-05-18T11:04:48Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "0210e6ef167fb2a92f91b7f082dc89cd8dbf6119", "job_id": 76511744387, "labels": ["ubuntu-latest"], "name": "deploy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029576112", "run_id": 26029576112, "runner_name": "GitHub Actions 1000478418", "started_at": "2026-05-18T11:04:51Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T11:04:33Z", "conclusion": "success", "created_at": "2026-05-18T10:58:57Z", "event": "release", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76510825111, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029321520", "run_id": 26029321520, "runner_name": "GitHub Actions 1000478403", "started_at": "2026-05-18T10:58:59Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:58:48Z", "conclusion": "success", "created_at": "2026-05-18T10:57:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76510612306, "labels": ["ubuntu-24.04"], "name": "Validate packages", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029258445", "run_id": 26029258445, "runner_name": "GitHub Actions 1000478401", "started_at": "2026-05-18T10:57:38Z", "status": "completed", "workflow_id": 12159822, "workflow_name": "Validate and Publish Release", "workflow_path": ".github/workflows/validate_and_publish_release.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:59:40Z", "conclusion": "success", "created_at": "2026-05-18T10:58:48Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76510802847, "labels": ["ubuntu-24.04"], "name": "Publish release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26029258445", "run_id": 26029258445, "runner_name": "GitHub Actions 1000478402", "started_at": "2026-05-18T10:58:51Z", "status": "completed", "workflow_id": 12159822, "workflow_name": "Validate and Publish Release", "workflow_path": ".github/workflows/validate_and_publish_release.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T10:42:33Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508255772, "labels": ["macos-14"], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028570221", "run_id": 26028570221, "runner_name": "GitHub Actions 1000478381", "started_at": "2026-05-18T10:42:37Z", "status": "in_progress", "workflow_id": 137427856, "workflow_name": "CI - macOS arm64 clang", "workflow_path": ".github/workflows/ci_macos_arm64_clang.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T10:42:30Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508248773, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028568338", "run_id": 26028568338, "runner_name": "GitHub Actions 1000478380", "started_at": "2026-05-18T10:42:33Z", "status": "in_progress", "workflow_id": 109041211, "workflow_name": "CI - Linux x64 gcc", "workflow_path": ".github/workflows/ci_linux_x64_gcc.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T10:42:30Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508248540, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028568277", "run_id": 26028568277, "runner_name": "GitHub Actions 1000478379", "started_at": "2026-05-18T10:42:32Z", "status": "in_progress", "workflow_id": 109041212, "workflow_name": "CI - Linux x64 clang BYO LLVM", "workflow_path": ".github/workflows/ci_linux_x64_clang_byollvm.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T11:34:37Z", "conclusion": "success", "created_at": "2026-05-18T10:42:29Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508245620, "labels": ["ah-ubuntu_22_04-c7g_4x-50"], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028567341", "run_id": 26028567341, "runner_name": "arm-hosted_i-006508e3c7297de63", "started_at": "2026-05-18T10:43:51Z", "status": "completed", "workflow_id": 108693130, "workflow_name": "CI - Linux arm64 clang", "workflow_path": ".github/workflows/ci_linux_arm64_clang.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T11:00:27Z", "conclusion": "success", "created_at": "2026-05-18T10:42:28Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508242670, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028566353", "run_id": 26028566353, "runner_name": "azure-linux-scale-rfhjf-runner-mtbth", "started_at": "2026-05-18T10:42:29Z", "status": "completed", "workflow_id": 109054114, "workflow_name": "CI - Linux x64 clang TSan", "workflow_path": ".github/workflows/ci_linux_x64_clang_tsan.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T10:42:25Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508234902, "labels": ["macos-15-intel"], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028563968", "run_id": 26028563968, "runner_name": "GitHub Actions 1000478378", "started_at": "2026-05-18T10:42:27Z", "status": "in_progress", "workflow_id": 109041301, "workflow_name": "CI - macOS x64 clang", "workflow_path": ".github/workflows/ci_macos_x64_clang.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T11:08:57Z", "conclusion": "success", "created_at": "2026-05-18T10:41:55Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508159501, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028541865", "run_id": 26028541865, "runner_name": "azure-linux-scale-rfhjf-runner-q88v9", "started_at": "2026-05-18T10:41:56Z", "status": "completed", "workflow_id": 109041213, "workflow_name": "CI - Linux x64 clang debug", "workflow_path": ".github/workflows/ci_linux_x64_clang_debug.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:55:49Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426299, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478359", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T09:13:17Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426302, "labels": ["macos-14"], "name": "macos :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478366", "started_at": "2026-05-18T06:30:38Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T09:12:11Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426307, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478358", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:33:37Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426312, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478360", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:57:26Z", "conclusion": "success", "created_at": "2026-05-18T06:30:35Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76470426320, "labels": ["windows-2022"], "name": "windows :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478363", "started_at": "2026-05-18T06:30:37Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T13:37:11.742131Z", "commit_message": "[Codegen] Remove deprecated transform.iree.match_callback tests (#24500)\n\nPart of #24466 (sub-task: remove deprecated tests under\n`compiler/src/iree/compiler/Codegen/Common/test/`).\n\nRemoves 10 lit tests that depend on the retired\n`transform.iree.match_callback` op and its supporting machinery\n(`register_match_callbacks`, `take_first`, `emit_remark`) from\n`llvm-external-projects/iree-dialects`.\n\n- 7 files use the deprecated op directly\n- 3 driver tests (`batch_matmuls.mlir`, `convolutions.mlir`,\n`reductions.mlir`) have RUN lines that only invoke the deleted\n`*_spec.mlir` files\n- `BUILD.bazel` and `CMakeLists.txt` updated to drop matching\nsrcs/exclude/data entries\n- Net diff: 12 files changed, 933 deletions(-), 0 insertions(+)\n- No remaining source-tree references to the deleted files\n\nSigned-off-by: Alex-Wengg <hanweng9@gmail.com>", "completed_at": "2026-05-18T10:57:34Z", "conclusion": "success", "created_at": "2026-05-18T10:57:27Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260518", "head_sha": "3f160df00a66b32fecbf5d0bab0905d24dc6a991", "job_id": 76510591999, "labels": ["ubuntu-24.04"], "name": "Trigger validate and publish release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26017376956", "run_id": 26017376956, "runner_name": "GitHub Actions 1000478400", "started_at": "2026-05-18T10:57:30Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:46:21Z", "conclusion": "success", "created_at": "2026-05-18T18:44:19Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595135356, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318650", "run_id": 26053318650, "runner_name": "GitHub Actions 1000478695", "started_at": "2026-05-18T18:44:54Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:46:48Z", "conclusion": "failure", "created_at": "2026-05-18T18:44:19Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595135374, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318650", "run_id": 26053318650, "runner_name": "GitHub Actions 1000478698", "started_at": "2026-05-18T18:46:20Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:47:22Z", "conclusion": "failure", "created_at": "2026-05-18T18:44:19Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595135406, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318650", "run_id": 26053318650, "runner_name": "GitHub Actions 1000478700", "started_at": "2026-05-18T18:46:52Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:45:18Z", "conclusion": "success", "created_at": "2026-05-18T18:44:20Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595139220, "labels": ["ubuntu-latest"], "name": "build", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318227", "run_id": 26053318227, "runner_name": "GitHub Actions 1000478696", "started_at": "2026-05-18T18:45:08Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:47:29Z", "conclusion": "success", "created_at": "2026-05-18T18:45:18Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595316494, "labels": ["ubuntu-latest"], "name": "report-build-status", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318227", "run_id": 26053318227, "runner_name": "GitHub Actions 1000478702", "started_at": "2026-05-18T18:47:25Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 3309b33 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T18:49:39Z", "conclusion": "success", "created_at": "2026-05-18T18:45:18Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "d7b9f90cef1e4017d8f65ecb7e3070af1ac9323d", "job_id": 76595316538, "labels": ["ubuntu-latest"], "name": "deploy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053318227", "run_id": 26053318227, "runner_name": "GitHub Actions 1000478710", "started_at": "2026-05-18T18:49:28Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:39:52Z", "conclusion": "success", "created_at": "2026-05-18T18:39:39Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594338359, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478668", "started_at": "2026-05-18T18:39:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:08Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378282, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478671", "started_at": "2026-05-18T18:40:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:43:15Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378289, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478679", "started_at": "2026-05-18T18:41:23Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:54:50Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378301, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478688", "started_at": "2026-05-18T18:42:53Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:51:52Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378309, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478684", "started_at": "2026-05-18T18:42:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:50:52Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378344, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-nxrrl", "started_at": "2026-05-18T18:45:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:47:00Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378345, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478682", "started_at": "2026-05-18T18:41:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:50Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378356, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478683", "started_at": "2026-05-18T18:41:51Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:53:22Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378387, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478681", "started_at": "2026-05-18T18:41:32Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:42Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378395, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478677", "started_at": "2026-05-18T18:41:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:44:51Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378407, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478687", "started_at": "2026-05-18T18:42:45Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:45:05Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378410, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478690", "started_at": "2026-05-18T18:43:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:53:47Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378416, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478685", "started_at": "2026-05-18T18:42:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:43:58Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378420, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478686", "started_at": "2026-05-18T18:42:27Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378421, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-67glz", "started_at": "2026-05-18T18:48:42Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378433, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_tsan / linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-75fjt", "started_at": "2026-05-18T18:48:39Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:24Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378451, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478673", "started_at": "2026-05-18T18:41:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:57Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378454, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478680", "started_at": "2026-05-18T18:41:23Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378461, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc / linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478678", "started_at": "2026-05-18T18:41:21Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378466, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm / linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478676", "started_at": "2026-05-18T18:41:15Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378469, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-j99xh", "started_at": "2026-05-18T18:49:07Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378489, "labels": ["macos-15-intel"], "name": "macos_x64_clang / macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478670", "started_at": "2026-05-18T18:40:05Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378496, "labels": ["ah-ubuntu_22_04-c7g_4x-50"], "name": "linux_arm64_clang / linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "arm-hosted_i-0d68db97c6f93d424", "started_at": "2026-05-18T18:41:19Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378499, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-ldwrk", "started_at": "2026-05-18T18:48:31Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378500, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "", "started_at": "2026-05-18T18:39:53Z", "status": "queued", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:56:13Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378529, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-55646", "started_at": "2026-05-18T18:48:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378548, "labels": ["macos-14"], "name": "macos_arm64_clang / macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478675", "started_at": "2026-05-18T18:41:11Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:43:51Z", "conclusion": "success", "created_at": "2026-05-18T18:39:32Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594317363, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053084624", "run_id": 26053084624, "runner_name": "GitHub Actions 1000478667", "started_at": "2026-05-18T18:39:37Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:39:35Z", "conclusion": "success", "created_at": "2026-05-18T18:39:25Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594297169, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478665", "started_at": "2026-05-18T18:39:27Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:52:16Z", "conclusion": "success", "created_at": "2026-05-18T18:39:36Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594329788, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "azure-linux-scale-gtvf9-runner-d7g94", "started_at": "2026-05-18T18:44:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596556936, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:55:40Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596556988, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478726", "started_at": "2026-05-18T18:53:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557005, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557056, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:57:16Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557061, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-79cxb", "started_at": "2026-05-18T18:55:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557127, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478728", "started_at": "2026-05-18T18:53:56Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557153, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": null, "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:57:11Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557196, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478721", "started_at": "2026-05-18T18:52:46Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557209, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557226, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557229, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557236, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557237, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557248, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478730", "started_at": "2026-05-18T18:54:29Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557254, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557261, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:57:01Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557281, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478727", "started_at": "2026-05-18T18:53:50Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557283, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:54:17Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557314, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478725", "started_at": "2026-05-18T18:53:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557317, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557319, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557323, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557338, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557340, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478722", "started_at": "2026-05-18T18:52:50Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557348, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557359, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:55:19Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557366, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478729", "started_at": "2026-05-18T18:54:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557377, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478731", "started_at": "2026-05-18T18:54:53Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557386, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:52:17Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557392, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "", "started_at": "2026-05-18T18:52:16Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:41:36Z", "conclusion": "success", "created_at": "2026-05-18T18:39:28Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594307235, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078732", "run_id": 26053078732, "runner_name": "GitHub Actions 1000478666", "started_at": "2026-05-18T18:39:31Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:41:21Z", "conclusion": "success", "created_at": "2026-05-18T18:39:39Z", "event": "dynamic", "head_branch": "refs/pull/24463/head", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594338833, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053076783", "run_id": 26053076783, "runner_name": "GitHub Actions 1000478672", "started_at": "2026-05-18T18:40:33Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24463", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:40:58Z", "conclusion": "success", "created_at": "2026-05-18T18:39:39Z", "event": "dynamic", "head_branch": "refs/pull/24463/head", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594338856, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053076783", "run_id": 26053076783, "runner_name": "GitHub Actions 1000478669", "started_at": "2026-05-18T18:39:55Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24463", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:42:01Z", "conclusion": "success", "created_at": "2026-05-18T18:39:39Z", "event": "dynamic", "head_branch": "refs/pull/24463/head", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594338861, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053076783", "run_id": 26053076783, "runner_name": "GitHub Actions 1000478674", "started_at": "2026-05-18T18:41:04Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24463", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:18Z", "conclusion": "success", "created_at": "2026-05-18T18:39:07Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594243858, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478650", "started_at": "2026-05-18T18:39:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:51:25Z", "conclusion": "success", "created_at": "2026-05-18T18:39:18Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594277147, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "azure-linux-scale-gtvf9-runner-85sdl", "started_at": "2026-05-18T18:44:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:25Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404222, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:25Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:53:53Z", "conclusion": "success", "created_at": "2026-05-18T18:51:25Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404246, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478718", "started_at": "2026-05-18T18:51:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404252, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478714", "started_at": "2026-05-18T18:51:28Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404260, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404289, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:56:59Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404291, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:53:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404296, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478719", "started_at": "2026-05-18T18:51:56Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404326, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404327, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404347, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:52:49Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404351, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478717", "started_at": "2026-05-18T18:51:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404353, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404374, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:54:56Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404377, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-msml5", "started_at": "2026-05-18T18:53:39Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404385, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404394, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:54:51Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404411, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478716", "started_at": "2026-05-18T18:51:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404415, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:54:26Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404426, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478715", "started_at": "2026-05-18T18:51:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404436, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-xxg82", "started_at": "2026-05-18T18:53:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:57:13Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404437, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478724", "started_at": "2026-05-18T18:52:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404439, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:57:17Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404447, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478723", "started_at": "2026-05-18T18:52:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404448, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:53:02Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404452, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478720", "started_at": "2026-05-18T18:51:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404471, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-p2v97", "started_at": "2026-05-18T18:57:23Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404472, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404474, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:52:45Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404494, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404665, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "", "started_at": "2026-05-18T18:51:26Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "success", "created_at": "2026-05-18T18:39:07Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594243281, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478649", "started_at": "2026-05-18T18:39:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:29Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271346, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478652", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:40:29Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271353, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478654", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:21Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271370, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478651", "started_at": "2026-05-18T18:39:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:51:52Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271372, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478655", "started_at": "2026-05-18T18:39:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:49:30Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271377, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478658", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:49Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271429, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478662", "started_at": "2026-05-18T18:39:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:44:01Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271430, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478653", "started_at": "2026-05-18T18:39:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:08Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271441, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478661", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:51:12Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271442, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478657", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:18Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271458, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478660", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:13Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271465, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478659", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271468, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-m2nvm", "started_at": "2026-05-18T18:41:06Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:13Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271506, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478664", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:56:56Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271532, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-qvnpz", "started_at": "2026-05-18T18:44:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271536, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-windows-scale-4lxzk-runner-dtrsg", "started_at": "2026-05-18T18:55:31Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:41:02Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271546, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478663", "started_at": "2026-05-18T18:39:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:48:00Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271552, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-snzv9", "started_at": "2026-05-18T18:41:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271556, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-wn8gf", "started_at": "2026-05-18T18:41:55Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:49:23Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271579, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478656", "started_at": "2026-05-18T18:39:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271604, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-8b7vt", "started_at": "2026-05-18T18:43:11Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271969, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594272218, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594272269, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594272343, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594272353, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:39:17Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594272378, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": null, "started_at": "2026-05-18T18:39:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:44:13Z", "conclusion": "success", "created_at": "2026-05-18T18:38:55Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594210743, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053050640", "run_id": 26053050640, "runner_name": "GitHub Actions 1000478648", "started_at": "2026-05-18T18:38:57Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:40:05Z", "conclusion": "success", "created_at": "2026-05-18T18:38:46Z", "event": "dynamic", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594184633, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053042788", "run_id": 26053042788, "runner_name": "GitHub Actions 1000478645", "started_at": "2026-05-18T18:38:50Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:40:02Z", "conclusion": "success", "created_at": "2026-05-18T18:38:46Z", "event": "dynamic", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594184638, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053042788", "run_id": 26053042788, "runner_name": "GitHub Actions 1000478646", "started_at": "2026-05-18T18:38:49Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:39:40Z", "conclusion": "success", "created_at": "2026-05-18T18:38:46Z", "event": "dynamic", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594184672, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053042788", "run_id": 26053042788, "runner_name": "GitHub Actions 1000478647", "started_at": "2026-05-18T18:38:48Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:20Z", "conclusion": "success", "created_at": "2026-05-18T18:26:10Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592000310, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478629", "started_at": "2026-05-18T18:26:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:48:00Z", "conclusion": "success", "created_at": "2026-05-18T18:26:20Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592029964, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "azure-linux-scale-gtvf9-runner-gczg7", "started_at": "2026-05-18T18:40:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796720, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-rl4qh", "started_at": "2026-05-18T18:57:02Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796733, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:56:09Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796743, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796767, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478712", "started_at": "2026-05-18T18:49:41Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:50:09Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796807, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478708", "started_at": "2026-05-18T18:48:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:56:53Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796815, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-5tfvl", "started_at": "2026-05-18T18:48:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796840, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:56:24Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796845, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-f8zwl", "started_at": "2026-05-18T18:51:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:51:37Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796869, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478709", "started_at": "2026-05-18T18:49:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796876, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796880, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796883, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:51:39Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796907, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478707", "started_at": "2026-05-18T18:48:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796910, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-fnqdv", "started_at": "2026-05-18T18:48:11Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:53:29Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796922, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-xjgrn", "started_at": "2026-05-18T18:52:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:51:33Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796923, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478705", "started_at": "2026-05-18T18:48:18Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:49:59Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796925, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:48:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:51:05Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796927, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478713", "started_at": "2026-05-18T18:50:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796933, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796936, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:53:31Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796940, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:50:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796948, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796951, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T18:53:19Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796952, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796954, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:52:42Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796969, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478706", "started_at": "2026-05-18T18:48:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:56:06Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796974, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:48:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796983, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "", "started_at": "2026-05-18T18:48:01Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:52:48Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796992, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478704", "started_at": "2026-05-18T18:48:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595797149, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478711", "started_at": "2026-05-18T18:49:32Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "success", "created_at": "2026-05-18T18:26:06Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76591990053, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478628", "started_at": "2026-05-18T18:26:08Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:31:10Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018820, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478631", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:38:26Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018835, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478633", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:28:14Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018836, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478630", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:28:30Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018844, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478638", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:35:15Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018857, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478632", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:28:17Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018889, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478637", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:27:54Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018897, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478635", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018903, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-nwsd8", "started_at": "2026-05-18T18:35:52Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:27:55Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018906, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478636", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:36:57Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018908, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478643", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:37:52Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018933, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478634", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:27:50Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018944, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478640", "started_at": "2026-05-18T18:26:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018947, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-7fdnc", "started_at": "2026-05-18T18:40:30Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:27:20Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018961, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478639", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018963, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-7drz6", "started_at": "2026-05-18T18:35:57Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018996, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-windows-scale-4lxzk-runner-s9ds7", "started_at": "2026-05-18T18:41:34Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019071, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-pbj6f", "started_at": "2026-05-18T18:40:11Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:28:51Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019163, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478642", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:27:38Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019164, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478641", "started_at": "2026-05-18T18:26:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019307, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019452, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019483, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019702, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019735, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019741, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:26:16Z", "conclusion": "skipped", "created_at": "2026-05-18T18:26:17Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019747, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": null, "started_at": "2026-05-18T18:26:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:28:04Z", "conclusion": "success", "created_at": "2026-05-18T18:26:00Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76591974681, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052392285", "run_id": 26052392285, "runner_name": "GitHub Actions 1000478626", "started_at": "2026-05-18T18:26:02Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:30:30Z", "conclusion": "success", "created_at": "2026-05-18T18:26:00Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76591975310, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052390774", "run_id": 26052390774, "runner_name": "GitHub Actions 1000478627", "started_at": "2026-05-18T18:26:04Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:11:17Z", "conclusion": "success", "created_at": "2026-05-18T18:05:44Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588487618, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051357912", "run_id": 26051357912, "runner_name": "GitHub Actions 1000478623", "started_at": "2026-05-18T18:05:48Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:47Z", "conclusion": "success", "created_at": "2026-05-18T18:05:36Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588463627, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478622", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:42:42Z", "conclusion": "success", "created_at": "2026-05-18T18:05:47Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588496569, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "azure-linux-scale-gtvf9-runner-859hv", "started_at": "2026-05-18T18:35:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869498, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478689", "started_at": "2026-05-18T18:43:00Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:13Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869517, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:44:50Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:47:34Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869524, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-v8lz7", "started_at": "2026-05-18T18:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:45:07Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869545, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-7n4f4", "started_at": "2026-05-18T18:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:46:18Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869547, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478693", "started_at": "2026-05-18T18:44:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:52:49Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869574, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478694", "started_at": "2026-05-18T18:44:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:53Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869577, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478692", "started_at": "2026-05-18T18:44:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:52:30Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869585, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:45:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:30Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869594, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478697", "started_at": "2026-05-18T18:45:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:49:25Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869595, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478699", "started_at": "2026-05-18T18:46:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869600, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:51:34Z", "status": "in_progress", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:06Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869603, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478691", "started_at": "2026-05-18T18:43:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:44:31Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869740, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:39Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869750, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478703", "started_at": "2026-05-18T18:47:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:45:31Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869755, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:43:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869767, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "", "started_at": "2026-05-18T18:42:43Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:53:19Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869773, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-vlsmd", "started_at": "2026-05-18T18:42:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:45:58Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869795, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:43:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:36Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869807, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:46:00Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:45:47Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869809, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-8dlx6", "started_at": "2026-05-18T18:42:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:43:44Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869822, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:42:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869827, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "", "started_at": "2026-05-18T18:42:43Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869844, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "", "started_at": "2026-05-18T18:42:43Z", "status": "queued", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:52:06Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869875, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-cckqb", "started_at": "2026-05-18T18:42:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:43:57Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869876, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:48:16Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869879, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478701", "started_at": "2026-05-18T18:47:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:45:47Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869887, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:44:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:44:48Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869899, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:43:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:53:12Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869900, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T18:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:51:28Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869914, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:45:49Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:40Z", "conclusion": "success", "created_at": "2026-05-18T18:05:23Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588427814, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343812", "run_id": 26051343812, "runner_name": "GitHub Actions 1000478606", "started_at": "2026-05-18T18:05:26Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "success", "created_at": "2026-05-18T18:05:23Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588428315, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478607", "started_at": "2026-05-18T18:05:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:10:21Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460071, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478615", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:06:38Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460112, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478608", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:34Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460151, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478618", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:08:09Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460172, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478611", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:16:04Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460188, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478613", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:15:51Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460221, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478620", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:00Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460267, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478609", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:16:31Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460279, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478619", "started_at": "2026-05-18T18:05:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:15Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460310, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478617", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:17:07Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460312, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478614", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:06:58Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460396, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478610", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:08:13Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460420, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478616", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:42:46Z", "conclusion": "success", "created_at": "2026-05-18T18:05:34Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460478, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-6vfbk", "started_at": "2026-05-18T18:05:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:42:46Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460487, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-mt4dw", "started_at": "2026-05-18T18:05:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460504, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-hg2zx", "started_at": "2026-05-18T18:34:04Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:46Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460505, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478621", "started_at": "2026-05-18T18:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:07:36Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460538, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478612", "started_at": "2026-05-18T18:05:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460585, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:33:44Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460678, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-windows-scale-4lxzk-runner-zghl4", "started_at": "2026-05-18T18:05:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": null, "conclusion": null, "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460708, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-vppzk", "started_at": "2026-05-18T18:26:59Z", "status": "in_progress", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460764, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460795, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460920, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588461023, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588461034, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:05:34Z", "conclusion": "skipped", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588461523, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": null, "started_at": "2026-05-18T18:05:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:44:37Z", "conclusion": "success", "created_at": "2026-05-18T17:40:52Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584198612, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050075026", "run_id": 26050075026, "runner_name": "GitHub Actions 1000478578", "started_at": "2026-05-18T17:40:54Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "success", "created_at": "2026-05-18T17:45:46Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585051769, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478582", "started_at": "2026-05-18T17:45:50Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:51:02Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101569, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478583", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:11Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101612, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478589", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:55Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101614, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478585", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:58:46Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101624, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478584", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:48:03Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101625, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478587", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:44Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101631, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478586", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:48:58Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101632, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478590", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:53Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101640, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478596", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:57:37Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101644, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478594", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:56:59Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101647, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478593", "started_at": "2026-05-18T17:46:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:48:20Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101666, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478592", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:58Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101673, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478588", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:09:40Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101691, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "azure-windows-scale-4lxzk-runner-hcvxc", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:55:33Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101700, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478595", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:09:36Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101711, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "azure-linux-scale-gtvf9-runner-vp9g7", "started_at": "2026-05-18T17:49:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:00:44Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101754, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "azure-linux-scale-gtvf9-runner-qn2rh", "started_at": "2026-05-18T17:47:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:56:50Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101762, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "azure-linux-scale-gtvf9-runner-srf59", "started_at": "2026-05-18T17:49:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:47:40Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101787, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478591", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:24:10Z", "conclusion": "success", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101805, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "azure-linux-scale-gtvf9-runner-s6f6q", "started_at": "2026-05-18T17:46:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101888, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101906, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101941, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585101987, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585102355, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585102383, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:46:03Z", "conclusion": "skipped", "created_at": "2026-05-18T17:46:03Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585102485, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": null, "started_at": "2026-05-18T17:46:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:24:21Z", "conclusion": "success", "created_at": "2026-05-18T18:24:11Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76591653644, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050068139", "run_id": 26050068139, "runner_name": "GitHub Actions 1000478625", "started_at": "2026-05-18T18:24:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:42:11Z", "conclusion": "success", "created_at": "2026-05-18T17:40:38Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584158966, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063377", "run_id": 26050063377, "runner_name": "GitHub Actions 1000478577", "started_at": "2026-05-18T17:40:41Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:41:28Z", "conclusion": "success", "created_at": "2026-05-18T17:41:16Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584270381, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478580", "started_at": "2026-05-18T17:41:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:48:49Z", "conclusion": "success", "created_at": "2026-05-18T17:41:28Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584305382, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "azure-linux-scale-gtvf9-runner-cjr8t", "started_at": "2026-05-18T17:41:30Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:55:51Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584505, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:51:08Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584521, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-lsfr8", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:50:57Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584558, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478601", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:06:29Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584560, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:02:58Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:53:47Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584581, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-22vd8", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:00:25Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584646, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:57:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:00:19Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584649, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:58:48Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:56:40Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584660, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-qzmdr", "started_at": "2026-05-18T17:48:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:58:46Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584661, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:55:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:04:05Z", "conclusion": "failure", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584664, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478597", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:54:00Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584686, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:02:56Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584699, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:00:27Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:08:22Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584711, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:01:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:03:02Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584725, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark01-ci", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:57:43Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584729, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478605", "started_at": "2026-05-18T17:48:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:01:31Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584755, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:00:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:53:15Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584763, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478599", "started_at": "2026-05-18T17:48:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:05:36Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584769, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:04:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:53:28Z", "conclusion": "success", "created_at": "2026-05-18T17:48:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584770, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478600", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:03:44Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584786, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark55-ci", "started_at": "2026-05-18T17:59:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:07:59Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584788, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-4vmll", "started_at": "2026-05-18T17:49:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:04:07Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584797, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:03:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:49:57Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584798, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478602", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:59:20Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584818, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark55-ci", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:50:06Z", "conclusion": "failure", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584840, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-kx7rm", "started_at": "2026-05-18T17:49:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:59:20Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584853, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T17:48:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:51:23Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584864, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478603", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:49:41Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584868, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478598", "started_at": "2026-05-18T17:48:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:52:04Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584882, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478604", "started_at": "2026-05-18T17:48:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:57:38Z", "conclusion": "success", "created_at": "2026-05-18T17:48:50Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76585584891, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:54:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T18:08:27Z", "conclusion": "failure", "created_at": "2026-05-18T18:08:23Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76588935859, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050063505", "run_id": 26050063505, "runner_name": "GitHub Actions 1000478624", "started_at": "2026-05-18T18:08:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:41:41Z", "conclusion": "success", "created_at": "2026-05-18T17:40:33Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584146214, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050058377", "run_id": 26050058377, "runner_name": "GitHub Actions 1000478574", "started_at": "2026-05-18T17:40:36Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:41:22Z", "conclusion": "success", "created_at": "2026-05-18T17:40:33Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584146217, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050058377", "run_id": 26050058377, "runner_name": "GitHub Actions 1000478576", "started_at": "2026-05-18T17:40:36Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[HIP] XFAIL stream transient buffer fill CTS", "completed_at": "2026-05-18T17:41:41Z", "conclusion": "success", "created_at": "2026-05-18T17:40:33Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "4c7d716519a8e85384ac6b0717233d313c0188ba", "job_id": 76584146230, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26050058377", "run_id": 26050058377, "runner_name": "GitHub Actions 1000478575", "started_at": "2026-05-18T17:40:36Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:19:09Z", "conclusion": "success", "created_at": "2026-05-18T17:17:05Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580078173, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048861990", "run_id": 26048861990, "runner_name": "GitHub Actions 1000478563", "started_at": "2026-05-18T17:17:07Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:20:34Z", "conclusion": "success", "created_at": "2026-05-18T17:16:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580032196, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048848491", "run_id": 26048848491, "runner_name": "GitHub Actions 1000478547", "started_at": "2026-05-18T17:16:52Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:16:59Z", "conclusion": "success", "created_at": "2026-05-18T17:16:46Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580021802, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478546", "started_at": "2026-05-18T17:16:49Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:24:12Z", "conclusion": "success", "created_at": "2026-05-18T17:16:59Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580060869, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "azure-linux-scale-gtvf9-runner-8pblf", "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:31:09Z", "conclusion": "failure", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320065, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:37:51Z", "conclusion": "failure", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320080, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478567", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:37:39Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320090, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:30:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:26:26Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320106, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-dbptp", "started_at": "2026-05-18T17:24:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:28:26Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320116, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-mhkzl", "started_at": "2026-05-18T17:24:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:25:33Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320127, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478564", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:32:21Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320134, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478568", "started_at": "2026-05-18T17:24:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:26:24Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320137, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478565", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:38Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320152, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "", "started_at": "2026-05-18T17:24:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:41:15Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320165, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:36:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:31:32Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320193, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-bqd9n", "started_at": "2026-05-18T17:24:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:39:17Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320202, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark55-ci", "started_at": "2026-05-18T17:29:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:34:42Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320228, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:33:02Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320230, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-j9nlq", "started_at": "2026-05-18T17:24:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:35:42Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320299, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark01-ci", "started_at": "2026-05-18T17:24:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:30:51Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320300, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:24:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:27:37Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320307, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478569", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:28:39Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320311, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478571", "started_at": "2026-05-18T17:24:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:25:01Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320313, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478566", "started_at": "2026-05-18T17:24:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:38Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320342, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "", "started_at": "2026-05-18T17:24:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:29:00Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320344, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478572", "started_at": "2026-05-18T17:24:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:29:16Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320349, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark55-ci", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:41:03Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320354, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark55-ci", "started_at": "2026-05-18T17:39:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:26:47Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320357, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478570", "started_at": "2026-05-18T17:24:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:41:06Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320361, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark75-ci", "started_at": "2026-05-18T17:37:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:35:58Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320374, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark10-ci", "started_at": "2026-05-18T17:31:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:38Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320381, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "", "started_at": "2026-05-18T17:24:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:25:19Z", "conclusion": "success", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320420, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-97jtp", "started_at": "2026-05-18T17:24:26Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:48Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320428, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "shark01-ci", "started_at": "2026-05-18T17:35:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:38Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:24:13Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76581320433, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "", "started_at": "2026-05-18T17:24:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:41:12Z", "conclusion": "failure", "created_at": "2026-05-18T17:41:06Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76584239959, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845196", "run_id": 26048845196, "runner_name": "GitHub Actions 1000478579", "started_at": "2026-05-18T17:41:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "success", "created_at": "2026-05-18T17:16:49Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580032439, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478548", "started_at": "2026-05-18T17:16:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:27:23Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065244, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478551", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:29:16Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065247, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478559", "started_at": "2026-05-18T17:17:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:05Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065251, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478556", "started_at": "2026-05-18T17:17:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:19:32Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065253, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478549", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:19:12Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065257, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478553", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:46Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065259, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478562", "started_at": "2026-05-18T17:17:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:43Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065267, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478554", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:20:29Z", "conclusion": "failure", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065279, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478557", "started_at": "2026-05-18T17:17:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:49Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065282, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478550", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:58Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065294, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478552", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:18:58Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065298, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478555", "started_at": "2026-05-18T17:17:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:19:10Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065300, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478561", "started_at": "2026-05-18T17:17:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:41:17Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065303, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "azure-linux-scale-gtvf9-runner-r99mn", "started_at": "2026-05-18T17:20:12Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:28:02Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065314, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478558", "started_at": "2026-05-18T17:17:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:40:14Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065323, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "azure-windows-scale-4lxzk-runner-fkfbv", "started_at": "2026-05-18T17:17:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:45:40Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065325, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "azure-linux-scale-gtvf9-runner-fvpqv", "started_at": "2026-05-18T17:22:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:26:58Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065326, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "azure-linux-scale-gtvf9-runner-cjdn5", "started_at": "2026-05-18T17:19:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:45:40Z", "conclusion": "cancelled", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065334, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "azure-linux-scale-gtvf9-runner-xxngf", "started_at": "2026-05-18T17:22:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:27:52Z", "conclusion": "success", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065343, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478560", "started_at": "2026-05-18T17:17:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065708, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065829, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065863, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580065895, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580066051, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580066143, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:01Z", "conclusion": "skipped", "created_at": "2026-05-18T17:17:01Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580066157, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": null, "started_at": "2026-05-18T17:17:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:45:45Z", "conclusion": "failure", "created_at": "2026-05-18T17:45:40Z", "event": "pull_request", "head_branch": "users/benvanik/vulkan2-follow", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76585035133, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048845177", "run_id": 26048845177, "runner_name": "GitHub Actions 1000478581", "started_at": "2026-05-18T17:45:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:41Z", "conclusion": "success", "created_at": "2026-05-18T17:16:39Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580004038, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048835956", "run_id": 26048835956, "runner_name": "GitHub Actions 1000478544", "started_at": "2026-05-18T17:16:42Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:44Z", "conclusion": "success", "created_at": "2026-05-18T17:16:39Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580004059, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048835956", "run_id": 26048835956, "runner_name": "GitHub Actions 1000478543", "started_at": "2026-05-18T17:16:42Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] Report shaderBFloat16 features", "completed_at": "2026-05-18T17:17:35Z", "conclusion": "success", "created_at": "2026-05-18T17:16:39Z", "event": "dynamic", "head_branch": "refs/pull/24505/head", "head_sha": "eceec46c023b74fd70f3a8b2327b748a4dc903a1", "job_id": 76580004142, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26048835956", "run_id": 26048835956, "runner_name": "GitHub Actions 1000478545", "started_at": "2026-05-18T17:16:42Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "PR #24505", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:09:04Z", "conclusion": "success", "created_at": "2026-05-18T15:08:51Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556326867, "labels": ["ubuntu-latest"], "name": "build", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113324", "run_id": 26042113324, "runner_name": "GitHub Actions 1000478520", "started_at": "2026-05-18T15:08:54Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:09:29Z", "conclusion": "success", "created_at": "2026-05-18T15:09:05Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556370790, "labels": ["ubuntu-latest"], "name": "deploy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113324", "run_id": 26042113324, "runner_name": "GitHub Actions 1000478522", "started_at": "2026-05-18T15:09:09Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:09:14Z", "conclusion": "success", "created_at": "2026-05-18T15:09:05Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556370824, "labels": ["ubuntu-latest"], "name": "report-build-status", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113324", "run_id": 26042113324, "runner_name": "GitHub Actions 1000478521", "started_at": "2026-05-18T15:09:09Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:10:15Z", "conclusion": "success", "created_at": "2026-05-18T15:08:47Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556311549, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113106", "run_id": 26042113106, "runner_name": "GitHub Actions 1000478517", "started_at": "2026-05-18T15:08:51Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:09:24Z", "conclusion": "failure", "created_at": "2026-05-18T15:08:47Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556311654, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113106", "run_id": 26042113106, "runner_name": "GitHub Actions 1000478518", "started_at": "2026-05-18T15:08:50Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Deployed 0653426 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T15:09:19Z", "conclusion": "failure", "created_at": "2026-05-18T15:08:47Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "9530a8f05ec34b8bfe05e0c94208d963f53c38a2", "job_id": 76556311832, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26042113106", "run_id": 26042113106, "runner_name": "GitHub Actions 1000478519", "started_at": "2026-05-18T15:08:49Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:08:45Z", "conclusion": "success", "created_at": "2026-05-18T15:01:26Z", "event": "schedule", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76554809026, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26041704282", "run_id": 26041704282, "runner_name": "GitHub Actions 1000478514", "started_at": "2026-05-18T15:01:30Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:26:07Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535561822, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "azure-linux-scale-rfhjf-runner-s9k59", "started_at": "2026-05-18T14:58:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:28:22Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818471, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-r8fv6", "started_at": "2026-05-18T15:26:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:34:56Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818495, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478532", "started_at": "2026-05-18T15:26:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:28:21Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818513, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478527", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:37:34Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818567, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark10-ci", "started_at": "2026-05-18T15:33:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:41:12Z", "conclusion": "failure", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818585, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478531", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:29:58Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818597, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-sntnn", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:36:51Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818608, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark75-ci", "started_at": "2026-05-18T15:32:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:34:03Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818609, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mzhnx", "started_at": "2026-05-18T15:26:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:33:34Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818634, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark10-ci", "started_at": "2026-05-18T15:30:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:36:06Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818714, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-m6sbv", "started_at": "2026-05-18T15:26:26Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:39:50Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818717, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:27:09Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818725, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-rcqjd", "started_at": "2026-05-18T15:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:30:26Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818728, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark55-ci", "started_at": "2026-05-18T15:27:46Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:27:44Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818732, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark55-ci", "started_at": "2026-05-18T15:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:30:21Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818737, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478533", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:28:35Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818743, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark01-ci", "started_at": "2026-05-18T15:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:29:10Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818748, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark75-ci", "started_at": "2026-05-18T15:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:27:15Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818786, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478534", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:40:39Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818787, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark10-ci", "started_at": "2026-05-18T15:37:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:27:16Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818790, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478535", "started_at": "2026-05-18T15:26:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:30:00Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818803, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark10-ci", "started_at": "2026-05-18T15:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:30:45Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818825, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478528", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:42:44Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818828, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark01-ci", "started_at": "2026-05-18T15:35:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:35:12Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818837, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark01-ci", "started_at": "2026-05-18T15:28:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:47:54Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818861, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark75-ci", "started_at": "2026-05-18T15:45:30Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:32:36Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818864, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark75-ci", "started_at": "2026-05-18T15:29:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:37:29Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818874, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark55-ci", "started_at": "2026-05-18T15:30:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:29:19Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818900, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478530", "started_at": "2026-05-18T15:26:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:45:27Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818914, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "shark75-ci", "started_at": "2026-05-18T15:36:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:28:49Z", "conclusion": "success", "created_at": "2026-05-18T15:26:07Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76559818929, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478529", "started_at": "2026-05-18T15:26:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:45:35Z", "conclusion": "failure", "created_at": "2026-05-18T15:45:28Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76563588828, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368777", "run_id": 26036368777, "runner_name": "GitHub Actions 1000478538", "started_at": "2026-05-18T15:45:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:37:14Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559679, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478477", "started_at": "2026-05-18T13:25:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:37:50Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559727, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478478", "started_at": "2026-05-18T13:25:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:13:07Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559820, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-linux-scale-rfhjf-runner-jss57", "started_at": "2026-05-18T14:44:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T14:51:06Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535559999, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-linux-scale-rfhjf-runner-bsgb9", "started_at": "2026-05-18T14:44:40Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T14:51:42Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560016, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-linux-scale-rfhjf-runner-wmznm", "started_at": "2026-05-18T14:44:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:21:10Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560099, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-linux-scale-rfhjf-runner-mdg96", "started_at": "2026-05-18T14:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T14:57:40Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560285, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-linux-scale-rfhjf-runner-54g42", "started_at": "2026-05-18T14:44:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T13:48:07Z", "conclusion": "success", "created_at": "2026-05-18T13:25:39Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76535560323, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "azure-windows-scale-4lxzk-runner-kk7tf", "started_at": "2026-05-18T13:29:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[LLVMCPU] Append user-specific cpu-features after the supported features from a cpu name. (#24473)\n\nBefore this, IREE will only use user-specific features and drop others.\n\nSigned-off-by: Jerry Shih <jerry.shih@sifive.com>", "completed_at": "2026-05-18T15:21:21Z", "conclusion": "success", "created_at": "2026-05-18T15:21:11Z", "event": "push", "head_branch": "main", "head_sha": "0653426d9f80eb0da38a85d93c79573864cf0049", "job_id": 76558842469, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26036368760", "run_id": 26036368760, "runner_name": "GitHub Actions 1000478525", "started_at": "2026-05-18T15:21:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:58:53Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655549, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-fgnws", "started_at": "2026-05-18T13:31:33Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T13:42:38Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655585, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-windows-scale-4lxzk-runner-jcz2f", "started_at": "2026-05-18T13:06:01Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:04:20Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655603, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-nc8tc", "started_at": "2026-05-18T13:32:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:22:57Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655681, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-grcf5", "started_at": "2026-05-18T13:53:49Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:12:25Z", "conclusion": "success", "created_at": "2026-05-18T13:04:41Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531655753, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "azure-linux-scale-rfhjf-runner-twbkg", "started_at": "2026-05-18T13:45:27Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:23:07Z", "conclusion": "success", "created_at": "2026-05-18T14:22:57Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76547014575, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035296996", "run_id": 26035296996, "runner_name": "GitHub Actions 1000478503", "started_at": "2026-05-18T14:23:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:28:01Z", "conclusion": "success", "created_at": "2026-05-18T13:04:42Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76531659482, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "azure-linux-scale-rfhjf-runner-rrd9l", "started_at": "2026-05-18T14:01:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:39:01Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035568, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark10-ci", "started_at": "2026-05-18T14:33:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:30:21Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035587, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-dm4h9", "started_at": "2026-05-18T14:28:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T15:03:20Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035605, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-2w2jq", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:37:18Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035689, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark75-ci", "started_at": "2026-05-18T14:32:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:43:31Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035713, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark75-ci", "started_at": "2026-05-18T14:37:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:43:02Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035724, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark10-ci", "started_at": "2026-05-18T14:39:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:49:54Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035742, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark75-ci", "started_at": "2026-05-18T14:43:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:43:52Z", "conclusion": "failure", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035745, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478512", "started_at": "2026-05-18T14:28:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:32:31Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035760, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478505", "started_at": "2026-05-18T14:28:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:36:51Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035769, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478504", "started_at": "2026-05-18T14:28:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:39:31Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035798, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark01-ci", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:42:05Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035803, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark55-ci", "started_at": "2026-05-18T14:36:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T15:01:30Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035804, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark75-ci", "started_at": "2026-05-18T14:49:56Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:36:06Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035838, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark55-ci", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:36:54Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035841, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-m65rl", "started_at": "2026-05-18T14:28:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:52:04Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035844, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark01-ci", "started_at": "2026-05-18T14:45:58Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:38:42Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035860, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:30:16Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035878, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478507", "started_at": "2026-05-18T14:28:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:33:11Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035885, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark10-ci", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:35:57Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035895, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-2tmd5", "started_at": "2026-05-18T14:28:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:32:42Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035905, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478508", "started_at": "2026-05-18T14:28:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:52:05Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035912, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark55-ci", "started_at": "2026-05-18T14:42:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:29:19Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035927, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478506", "started_at": "2026-05-18T14:28:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:32:21Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035980, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark75-ci", "started_at": "2026-05-18T14:28:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:31:37Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548035985, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478511", "started_at": "2026-05-18T14:28:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:45:56Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548036005, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark01-ci", "started_at": "2026-05-18T14:39:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:47:41Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548036033, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "shark10-ci", "started_at": "2026-05-18T14:43:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:29:15Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548036070, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-8d8l7", "started_at": "2026-05-18T14:28:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:30:53Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548036092, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478510", "started_at": "2026-05-18T14:28:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T14:29:06Z", "conclusion": "success", "created_at": "2026-05-18T14:28:02Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76548036128, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478509", "started_at": "2026-05-18T14:28:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Address PR feedback\n\nSigned-off-by: Lukas Sommer <lukas.sommer@amd.com>", "completed_at": "2026-05-18T15:01:37Z", "conclusion": "failure", "created_at": "2026-05-18T15:01:31Z", "event": "pull_request", "head_branch": "new-lds-promotion", "head_sha": "9dc1f483410bc449f6edf0b7be22cb91dd90d14e", "job_id": 76554825805, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26035297010", "run_id": 26035297010, "runner_name": "GitHub Actions 1000478515", "started_at": "2026-05-18T15:01:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:57:37Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451346, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-dnjxd", "started_at": "2026-05-18T13:30:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:53:06Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451431, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-9rr72", "started_at": "2026-05-18T13:30:48Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T14:14:33Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451491, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-j8jqj", "started_at": "2026-05-18T13:31:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T14:00:51Z", "conclusion": "success", "created_at": "2026-05-18T12:52:25Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76529451600, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "azure-linux-scale-rfhjf-runner-r2ts4", "started_at": "2026-05-18T13:31:23Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T14:14:39Z", "conclusion": "success", "created_at": "2026-05-18T14:14:33Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76545323949, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656897", "run_id": 26034656897, "runner_name": "GitHub Actions 1000478502", "started_at": "2026-05-18T14:14:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:45:10Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292388, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:38:40Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292453, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-mzws8", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:45:57Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292457, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark10-ci", "started_at": "2026-05-18T13:41:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:52:03Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292534, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-4bvgx", "started_at": "2026-05-18T13:49:42Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:50:24Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292552, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-zkxmr", "started_at": "2026-05-18T13:40:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:42:41Z", "conclusion": "success", "created_at": "2026-05-18T13:34:31Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292589, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark01-ci", "started_at": "2026-05-18T13:36:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:40:33Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292611, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark55-ci", "started_at": "2026-05-18T13:37:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:37:35Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292629, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark75-ci", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:44:51Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292654, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark75-ci", "started_at": "2026-05-18T13:37:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:45:52Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292684, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark01-ci", "started_at": "2026-05-18T13:42:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:38:22Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292690, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478494", "started_at": "2026-05-18T13:34:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:38:49Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292692, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478497", "started_at": "2026-05-18T13:34:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:49:31Z", "conclusion": "failure", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292711, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478496", "started_at": "2026-05-18T13:34:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:37:46Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292715, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478495", "started_at": "2026-05-18T13:34:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:47:23Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292718, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark75-ci", "started_at": "2026-05-18T13:44:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:42:18Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292721, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark55-ci", "started_at": "2026-05-18T13:40:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:41:50Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292731, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark10-ci", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:50:01Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292736, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark55-ci", "started_at": "2026-05-18T13:42:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:48:40Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292738, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark10-ci", "started_at": "2026-05-18T13:46:00Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:50:18Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292768, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark75-ci", "started_at": "2026-05-18T13:47:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:39:42Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292815, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-7fm66", "started_at": "2026-05-18T13:38:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:43:39Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292836, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-xfgnv", "started_at": "2026-05-18T13:35:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:37:32Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292872, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark55-ci", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:48:04Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537292967, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "shark01-ci", "started_at": "2026-05-18T13:45:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:43:22Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537293044, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478498", "started_at": "2026-05-18T13:34:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:39:01Z", "conclusion": "success", "created_at": "2026-05-18T13:34:32Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76537293092, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478500", "started_at": "2026-05-18T13:34:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "Merge promoted LDS operand layout changes\n\nMerge the promoted-read layout changes and compute swizzle hints from the coalesced global-read writer layout to the propagated consumer reader layout. Keep mismatched sub-bank accesses on padding, preserve scalar single-element resolver support for f32, and constrain flattening of swizzled allocs with bufferization deallocs.\n\nCo-authored-by: GPT-5 Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T13:52:09Z", "conclusion": "failure", "created_at": "2026-05-18T13:52:03Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "2bee97354aac6e473be612f58f744ad1d0321e56", "job_id": 76540793943, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26034656888", "run_id": 26034656888, "runner_name": "GitHub Actions 1000478501", "started_at": "2026-05-18T13:52:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T16:09:35Z", "conclusion": "success", "created_at": "2026-05-18T10:42:25Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508234902, "labels": ["macos-15-intel"], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028563968", "run_id": 26028563968, "runner_name": "GitHub Actions 1000478378", "started_at": "2026-05-18T10:42:27Z", "status": "completed", "workflow_id": 109041301, "workflow_name": "CI - macOS x64 clang", "workflow_path": ".github/workflows/ci_macos_x64_clang.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T16:30:10Z", "conclusion": "success", "created_at": "2026-05-18T10:42:30Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508248540, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028568277", "run_id": 26028568277, "runner_name": "GitHub Actions 1000478379", "started_at": "2026-05-18T10:42:32Z", "status": "completed", "workflow_id": 109041212, "workflow_name": "CI - Linux x64 clang BYO LLVM", "workflow_path": ".github/workflows/ci_linux_x64_clang_byollvm.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T14:57:17Z", "conclusion": "success", "created_at": "2026-05-18T10:42:30Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508248773, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028568338", "run_id": 26028568338, "runner_name": "GitHub Actions 1000478380", "started_at": "2026-05-18T10:42:33Z", "status": "completed", "workflow_id": 109041211, "workflow_name": "CI - Linux x64 gcc", "workflow_path": ".github/workflows/ci_linux_x64_gcc.yml"}
{"collected_at": "2026-05-18T18:57:22.649784Z", "commit_message": "[Vulkan] HAL Rewrite (#24459)\n\nThis should be compatible with old VMFBs. I expect some platforms to\npossibly need feature backfill or compatibility support but our testing\nstory is too thin to know yet. The big shift here is baselining on\nVulkan 1.3. With a bit of extra work we could stretch back to 1.2 with\nextensions.\n\n---\n\nThis PR replaces the legacy Vulkan HAL with a new implementation built\naround the current HAL queue contract, native Vulkan command recording,\nexplicit feature negotiation, buffer-device-address dispatch, direct\ntransfer/file paths, and HAL-native profiling.\n\nThe old driver worked (and was some of the oldest IREE code, written\nbefore it was even open sourced!), but it grew around synchronous\nsubmission, descriptor-set dispatch, and direct recording assumptions\nthat do not map well to modern IREE workloads. In particular, it did not\ngive us a clean way to model asynchronous queue frontiers, reusable\ncommand-buffer replay, file/transfer operations, BDA dispatch, or\nbackend-visible profiling statistics without continuing to layer\ncompatibility paths on top of the old design.\n\nThe new driver has two goals:\n\n- Keep descriptor ABI compatibility for existing Vulkan executables.\n- Make the optimized Vulkan path descriptor-free, replayable,\nobservable, and aligned with the same command-buffer model used by newer\nHAL backends.\n\n## Driver Architecture\n\nThe implementation is organized around explicit ownership boundaries:\n\n- `api.*` defines Vulkan HAL feature policy, extension names, and public\noption translation.\n- `physical_device.*` owns enumeration, device snapshots, baseline\nvalidation, extension discovery, feature/property capture, and\nqueue-family description.\n- `logical_device.*` owns logical-device creation, queue selection,\ntopology, allocator installation, executable-cache creation, file\nimport, and device queries.\n- `queue.*` owns queue submission, timeline epochs, dependency\nresolution, native command-buffer allocation, asynchronous completion,\nstaging, direct dispatch, transfer/file operation recording, profiling\ntimestamps, and replay cache statistics.\n- `command_buffer.*` owns reusable HAL command-buffer storage, compact\ncommand records, descriptor/BDA requirement accounting, resource\nlifetime tracking, and native replay into `VkCommandBuffer`.\n- `executable.*` owns Vulkan executable loading, SPIR-V module/pipeline\ncreation, descriptor ABI metadata, BDA ABI metadata, raw BDA executable\nsupport, global lookup, and per-pipeline dispatch metadata.\n\nUnsupported cases are rejected at the boundary where they are\ndiscovered. The rewrite intentionally avoids silent fallbacks that would\nhide missing Vulkan features or route hot paths through emulation.\n\n## Queue Model\n\nThe new queue implementation uses Vulkan timeline semaphores as queue\nepochs. Each selected queue lane tracks accepted submissions,\nsignal/wait frontiers, completion state, and pending cleanup. HAL\nsemaphore dependencies are resolved through the queue path without\nassuming FIFO submission order.\n\nQueue submission supports:\n\n- execution barriers;\n- queue allocation and deallocation;\n- sparse binding;\n- fill, update, copy, read, and write operations;\n- direct dispatch;\n- reusable command-buffer execute;\n- file-backed staging;\n- host callbacks.\n\nThis replaces the old split between synchronous direct operations and\nspecial driver-side helper paths. Queue work now enters through one\nscheduling model and retires through one completion model.\n\n## Executable And Dispatch ABIs\n\nThe driver supports both Vulkan executable ABIs:\n\n- `vulkan-spirv-fb`, the descriptor ABI, for compatibility with existing\ndescriptor-set storage-buffer executables.\n- `vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw`, the BDA ABIs, for\ndescriptor-free dispatch through a device-address binding table.\n\nThe descriptor ABI now uses `VK_KHR_push_descriptor` when the device\nsupports it and the pipeline layout fits the extension limits. That\nremoves descriptor pool allocation and descriptor-set update overhead\nfrom the descriptor compatibility path.\n\nBDA is the primary performance path. The runtime publishes a compact\ndevice-address binding table and the shader reads resource addresses\ndirectly. Reusable command buffers can keep the native Vulkan command\nbody resident while only the published BDA table bytes change between\nexecutions.\n\n## Device Compatibility\n\nThis rewrite intentionally raises the Vulkan baseline. The\ndriver-created instance requests Vulkan 1.3 by default, and\nphysical-device enumeration only accepts devices with:\n\n- `apiVersion >= VK_API_VERSION_1_3`;\n- `timelineSemaphore`;\n- `scalarBlockLayout`;\n- `synchronization2`;\n- at least one compute-capable queue family.\n\nThat baseline is what lets the queue use timeline-semaphore epochs,\nexpress barriers through synchronization2, and keep shader/storage\nlayouts consistent without carrying old synchronization and layout\ncompatibility paths. The target is Vulkan 1.3, not Vulkan 1.4; Vulkan\n1.3 has been the stable modern Vulkan baseline since [January\n2022](https://docs.vulkan.org/guide/latest/versions.html#vulkan-13-features-breakdown).\n\n`bufferDeviceAddress` is a second gate for the optimized BDA dispatch\nABI. A device can still use descriptor-ABI executables without BDA, but\n`vulkan-spirv-bda-v1` and `vulkan-spirv-bda-raw` require BDA because\ntheir binding table is a table of device addresses instead of descriptor\nbindings.\n\nOptional capabilities remain opportunistic:\n\n- `VK_KHR_push_descriptor` accelerates the descriptor compatibility path\nwhen available.\n- `VK_EXT_calibrated_timestamps` improves profile clock correlation when\navailable.\n- external memory, host memory import, sparse binding, and sparse\nresidency are enabled only when the device and requested HAL features\nrequire them.\n\nAs a rough ecosystem sanity check, the [Vulkan Hardware Database\n`apiVersion`\ndistribution](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion)\nreports Vulkan 1.3 or 1.4 in 24,376 of 44,893 submitted all-platform\nreports (54.3%). The desktop slice is stronger: 12,446 of 19,758\n[Windows\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=windows)\n(63.0%) and 9,547 of 15,964 [Linux\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=linux)\n(59.8%) are Vulkan 1.3+. Android is lower at 2,400 of 7,832 [Android\nreports](https://vulkan.gpuinfo.org/displaycoreproperty.php?name=apiversion&platform=android)\n(30.6%), which matches the fact that this rewrite is primarily aimed at\nmodern desktop/server Vulkan rather than old mobile Vulkan stacks.\n\nThe required feature bits are not the practical compatibility cliff\nwithin the modern driver cohort: gpuinfo.org's all-platform [Vulkan 1.2\nfeature coverage](https://vulkan.gpuinfo.org/listfeaturescore12.php) and\n[Vulkan 1.3 feature\ncoverage](https://vulkan.gpuinfo.org/listfeaturescore13.php) list\n`timelineSemaphore` at 99.88%, `scalarBlockLayout` at 98.82%,\n`synchronization2` at 99.82%, and `bufferDeviceAddress` at 98.47%. Treat\nthose numbers as [community-submitted device/report coverage rather than\nmarket share](https://vulkan.gpuinfo.org/about.php), but they are useful\nevidence that the meaningful bump is the Vulkan 1.3 driver baseline, not\nan obscure optional-feature combination.\n\n## Command Buffers\n\nHAL command buffers record structured Vulkan command records instead of\ntrying to expose old direct-command-buffer behavior through the HAL API.\nDuring recording the driver accumulates:\n\n- descriptor requirements for descriptor ABI replay;\n- BDA publication byte requirements for BDA replay;\n- retained resources for safe command-buffer lifetime;\n- dispatch metadata required for profiling and native replay.\n\nAt queue execution time the command buffer is replayed into a native\n`VkCommandBuffer`. BDA command buffers can use the native replay cache:\nthe cache keeps reusable native command buffers and BDA publication\nstorage per queue lane, skips publication when binding slots are\nunchanged, and exposes\nhits, misses, forks, bypasses, trims, peak instance count, and peak\npublication bytes through `query_i64` counters under\n`vulkan.queue.native_replay_cache`.\n\nDescriptor command buffers currently bypass native replay caching\nbecause their replayed Vulkan commands embed binding-table-dependent\ndescriptor state. This matches the old indirect-command-buffer shape:\nthe old driver recorded deferred HAL commands for binding-table command\nbuffers and materialized a native Vulkan command buffer during each\n`queue_execute`. The rewrite keeps that compatibility contract, uses\npush descriptors when available, and reserves resident native replay for\nBDA command buffers where the command body is independent of the\nexecute-time binding table.\n\n## Transfer And File Paths\n\nQueue transfers are native Vulkan operations in the new driver. Fill,\nupdate, and copy are recorded directly into Vulkan command buffers.\nUnaligned fill and update edges are patched with driver-owned built-in\npipelines instead of falling through an emulated transfer layer.\n\nFile reads and writes use storage-buffer-backed files directly when\navailable. Host-backed files are staged through device-visible memory\nand the async file path, keeping file operations on the same\nqueue/completion model as the rest of the driver.\n\n## Profiling And Statistics\n\nProfiling is built into the queue and command-buffer paths. The driver\nrecords HAL executable metadata, command-buffer metadata, queue events,\nqueue device timestamps, and optional per-dispatch timestamp ranges\nthrough the local HAL profile recorder used by `iree-profile`.\n\nTimestamp query pools are queue-owned and cached. Calibrated timestamp\nsupport is used when available to relate Vulkan device ticks to the HAL\nprofile timeline, and invalid calibrated ranges are marked instead of\nforcing a bad clock fit.\n\nThe driver also exposes backend statistics through device queries,\nincluding native replay cache behavior, BDA publication cache behavior,\nand timestamp query cache behavior. These counters make performance\npaths inspectable instead of requiring driver-internal debugging.\n\n## Performance Characterization\n\nThe performance goal of the rewrite is simple: Vulkan command buffers\nshould be cheap to submit because the backend records native Vulkan work\nonce and replays that work directly. The benchmark rows below keep that\ncontract visible by separating host submit cost from end-to-end drain\ncost:\n\n- Submit-only rows time command-buffer replay and queue submission, with\nthe final completion wait outside the timed region.\n- Submit + wait rows include the final completion wait and therefore\ninclude driver and device drain behavior.\n\nAll rows are release Google Benchmark medians. The benchmark set\ncompares the old descriptor-set Vulkan HAL, the new\ndescriptor-compatible Vulkan path, the new BDA Vulkan dispatch ABI, and\nAMDGPU AQL/PM4 command-buffer replay. Those are distinct mechanisms, so\nthe tables keep them separate instead of turning the result into a\nsingle backend ranking.\n\n### Vulkan Command-Buffer Replay\n\nHomogeneous 10K-dispatch command-buffer replay:\n\n| Path | ABI | Submit-only | Submit + wait | Interpretation |\n| --- | --- | ---: | ---: | --- |\n| Old Vulkan HAL | Descriptor sets | 5.139 ms | 5.609 ms | Deferred HAL\ncommand buffer replayed into Vulkan and bound descriptor sets for every\ndispatch. |\n| New Vulkan HAL | Push descriptors | 2.072 ms | 5.257 ms | Descriptor\ncompatibility path avoids descriptor pool/set churn and cuts host replay\ncost by 2.48x. |\n| New Vulkan HAL | BDA | 12.361 us | 1.435 ms | Native BDA replay hits\nthe native replay cache and cuts host replay cost by 416x vs old Vulkan.\n|\n\nThe descriptor row is the compatibility story: the new HAL is still\nfaster than old Vulkan when running the legacy descriptor ABI. The BDA\nrow is the design story: when the compiler targets the new ABI, replay\nbecomes one cached native command-buffer submission instead of a\nper-dispatch descriptor-binding loop.\n\nFor command-buffer recording, the hot unvalidated/unretained path\nremains close to the old implementation, and the default checked path is\nnow within about 24 us over 5K dispatches:\n\n| Path | Recording mode | 5K-dispatch record-only |\n| --- | --- | ---: |\n| Old Vulkan HAL | Default validation/retention | 183.547 us |\n| Old Vulkan HAL | Unvalidated + unretained | 93.925 us |\n| New Vulkan HAL | Default validation/retention | 207.266 us |\n| New Vulkan HAL | Unvalidated, retained | 113.191 us |\n| New Vulkan HAL | Validated, unretained | 190.903 us |\n| New Vulkan HAL | Unvalidated + unretained | 97.998 us |\n\nThat distinction matters for interpreting the submit rows. The rewrite\nis not winning by making unchecked recording magically cheaper; it is\nwinning because recorded BDA command buffers replay as native Vulkan\nwork instead of walking the IREE deferred command stream on every\nsubmission. The record-only default delta is validation and retention\nbookkeeping on a cold path; the repeated-use win is in the submit/replay\npath.\n\n### Direct Transfer Host Cost\n\nQueue transfer operations now go through native queue paths instead of\nthe old host-side emulated command-buffer path. The strong claim here is\nsubmit-only host overhead:\n\n| Operation | Size | Old Vulkan submit-only | New Vulkan submit-only |\nSpeedup |\n| --- | ---: | ---: | ---: | ---: |\n| Copy batch x20 | 4 B | 304 us | 46 us | 6.54x |\n| Copy batch x20 | 4 KiB | 309 us | 57 us | 5.47x |\n| Fill batch x20 | 4 B | 295 us | 48 us | 6.09x |\n| Fill batch x20 | 4 KiB | 285 us | 57 us | 5.02x |\n| Update batch x20 | 4 B | 278 us | 59 us | 4.75x |\n| Update batch x20 | 4 KiB | 291 us | 98 us | 2.98x |\n\n### RADV BDA vs AMDGPU AQL/PM4 ABABA\n\nThe ABABA benchmark records 10K alternating dispatches across two\npipelines with 100 binding-table entries and a 20% overlap grouping\npattern. It is designed to stress replay, binding publication, and\ndriver/device drain behavior instead of a single homogeneous pipeline\nloop.\n\n| Path | Submit-only | Submit + wait | Interpretation |\n| --- | ---: | ---: | --- |\n| Vulkan BDA on RADV | 36.288 us | 47.190 ms | HAL-side replay is cheap,\nbut public Vulkan/RADV drains the alternating-pipeline sequence slowly.\n|\n| AMDGPU AQL dynamic | 138.183 us | 41.484 ms | Low host submit cost,\nbut device drain still follows AQL packet-stream behavior. |\n| AMDGPU AQL static | 98.677 us | 41.075 ms | Static publication helps\nsubmit, while drain remains AQL-like. |\n| AMDGPU PM4 static | 1.445 us | 5.076 ms | One resident PM4 IB packet\ngives the lowest submit cost and much lower drain time. |\n| AMDGPU PM4 fixup | 7.658 us | 5.194 ms | Dynamic binding-table update\nvia GPU fixup plus PM4 IB replay stays close to static PM4. |\n\nThis is the backend mechanism comparison. Vulkan BDA gets the Vulkan HAL\nout of descriptor emulation and into native replay. AMDGPU PM4 shows the\nnext layer: when the backend owns the low-level command stream below the\npublic driver API, the same recorded-work shape can drain far faster.\nThis is also a useful RADV-vs-PM4 ABABA contrast: the HAL-side Vulkan\nreplay is already cheaper than AQL submit, while PM4 shows what the\ndevice-side alternating-pipeline ABABA drain can look like when the\nbackend bypasses the public Vulkan driver stack.\n\nThe long RADV ABABA drain is not a regression in the HAL barrier\nlowering. A profiled ABABA run recorded one queue-device interval of\nroughly 40 ms for the same 10K alternating-dispatch sequence, while the\nhost submit-to-ready span was only a few milliseconds. The\ncommand-buffer metadata showed the expected 10K alternating dispatches\nand execution-only barriers, and the sync2 lowering now preserves\nexecution ordering without synthesizing memory read/write visibility.\n\n## Highlights\n\nThis rewrite turns Vulkan from a synchronous, descriptor-heavy\ncompatibility backend into a modern HAL backend:\n\n- asynchronous queue semantics using timeline-semaphore epochs and\nexplicit frontiers;\n- native queue paths for transfer, dispatch, file staging, sparse\nbinding, command-buffer execute, and host calls;\n- descriptor ABI compatibility plus a BDA ABI that materially improves\ncommand-buffer replay scaling;\n- reusable native Vulkan command-buffer replay for BDA dispatches;\n- HAL-native queue, command-buffer, executable, and dispatch profiling\nthrough `iree-profile`;\n- queryable backend statistics for replay, BDA publication, and\ntimestamp caches;\n- stricter unsupported-case handling instead of compatibility-shaped\nsilent fallback behavior;\n- No more C++.\n\nTLDR: the descriptor compatibility path is already materially better\nthan old Vulkan, the BDA path is the intended fast path and gives Vulkan\nnative command-buffer replay instead of deferred HAL replay, direct\ntransfers no longer bounce through emulated command buffers, and the\nbackend now has the profiling/statistics hooks needed to keep tightening\nthe hot paths with hard evidence.", "completed_at": "2026-05-18T14:33:52Z", "conclusion": "success", "created_at": "2026-05-18T10:42:33Z", "event": "schedule", "head_branch": "main", "head_sha": "b46def4476d709db249406e0ca3b9a59a16a5196", "job_id": 76508255772, "labels": ["macos-14"], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26028570221", "run_id": 26028570221, "runner_name": "GitHub Actions 1000478381", "started_at": "2026-05-18T10:42:37Z", "status": "completed", "workflow_id": 137427856, "workflow_name": "CI - macOS arm64 clang", "workflow_path": ".github/workflows/ci_macos_arm64_clang.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:53:21Z", "conclusion": "failure", "created_at": "2026-05-18T20:52:50Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616873799, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821898", "run_id": 26059821898, "runner_name": "GitHub Actions 1000478763", "started_at": "2026-05-18T20:52:53Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:54:10Z", "conclusion": "success", "created_at": "2026-05-18T20:52:50Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616873810, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821898", "run_id": 26059821898, "runner_name": "GitHub Actions 1000478762", "started_at": "2026-05-18T20:52:52Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:53:25Z", "conclusion": "failure", "created_at": "2026-05-18T20:52:50Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616873816, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821898", "run_id": 26059821898, "runner_name": "GitHub Actions 1000478764", "started_at": "2026-05-18T20:52:53Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:53:01Z", "conclusion": "success", "created_at": "2026-05-18T20:52:50Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616871735, "labels": ["ubuntu-latest"], "name": "build", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821160", "run_id": 26059821160, "runner_name": "GitHub Actions 1000478761", "started_at": "2026-05-18T20:52:52Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:53:10Z", "conclusion": "success", "created_at": "2026-05-18T20:53:02Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616904570, "labels": ["ubuntu-latest"], "name": "report-build-status", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821160", "run_id": 26059821160, "runner_name": "GitHub Actions 1000478765", "started_at": "2026-05-18T20:53:06Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Deployed d9a3dd1 with MkDocs version: 1.6.1", "completed_at": "2026-05-18T20:53:15Z", "conclusion": "success", "created_at": "2026-05-18T20:53:02Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "8f26eb932903d4ca8e77f792f41efaa03b26c16a", "job_id": 76616904674, "labels": ["ubuntu-latest"], "name": "deploy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059821160", "run_id": 26059821160, "runner_name": "GitHub Actions 1000478766", "started_at": "2026-05-18T20:53:05Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:52:49Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002256, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564546", "run_id": 26059564546, "runner_name": "GitHub Actions 1000478744", "started_at": "2026-05-18T20:47:46Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002452, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478745", "started_at": "2026-05-18T20:47:46Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:52:54Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031691, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478748", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:00:02Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031708, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478752", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:50:57Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031718, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478754", "started_at": "2026-05-18T20:47:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:50:03Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031723, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478750", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:03Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031736, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478749", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:15:11Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031760, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-linux-scale-gtvf9-runner-6xw48", "started_at": "2026-05-18T20:48:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:50:42Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031802, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478759", "started_at": "2026-05-18T20:48:40Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:21Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031808, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478751", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:59:44Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031814, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478756", "started_at": "2026-05-18T20:47:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:14:55Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031824, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-linux-scale-gtvf9-runner-jmgv4", "started_at": "2026-05-18T20:47:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:33Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031828, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478747", "started_at": "2026-05-18T20:47:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:07:37Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031842, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-windows-scale-4lxzk-runner-md82p", "started_at": "2026-05-18T20:47:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:00:15Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031847, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-linux-scale-gtvf9-runner-sjzz5", "started_at": "2026-05-18T20:48:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:22Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031848, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478757", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:16Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031849, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478755", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:50:45Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031851, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478760", "started_at": "2026-05-18T20:48:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:55Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031857, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478758", "started_at": "2026-05-18T20:47:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:54:37Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031870, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-linux-scale-gtvf9-runner-t4s82", "started_at": "2026-05-18T20:48:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:59:01Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031874, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478753", "started_at": "2026-05-18T20:47:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:54:09Z", "conclusion": "success", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031897, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "azure-linux-scale-gtvf9-runner-l8g6k", "started_at": "2026-05-18T20:48:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616031984, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616032163, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616032207, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616032539, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616032567, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "skipped", "created_at": "2026-05-18T20:47:54Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616032609, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": null, "started_at": "2026-05-18T20:47:54Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:15:16Z", "conclusion": "success", "created_at": "2026-05-18T21:15:11Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76620526426, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564547", "run_id": 26059564547, "runner_name": "GitHub Actions 1000478776", "started_at": "2026-05-18T21:15:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:47:54Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002623, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478746", "started_at": "2026-05-18T20:47:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:55:18Z", "conclusion": "success", "created_at": "2026-05-18T20:47:55Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616033882, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "azure-linux-scale-gtvf9-runner-4dtq6", "started_at": "2026-05-18T20:48:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:58:50Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291093, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark10-ci", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:18:03Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291140, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark75-ci", "started_at": "2026-05-18T21:12:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:57:54Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291204, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-9j75t", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:03:49Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291221, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478771", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:59:07Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291241, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-x8jvp", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:57:33Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291261, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478767", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:10:15Z", "conclusion": "failure", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291323, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478769", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:04:51Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291343, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-bbf94", "started_at": "2026-05-18T20:55:26Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:06:38Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291368, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark10-ci", "started_at": "2026-05-18T21:04:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:56:36Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291423, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478773", "started_at": "2026-05-18T20:55:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:56:53Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291428, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark55-ci", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:59:32Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291430, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478768", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:04:12Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291440, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark10-ci", "started_at": "2026-05-18T21:01:49Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:59:37Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291455, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478770", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:05:02Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291467, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark55-ci", "started_at": "2026-05-18T21:03:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:04:47Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291477, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark01-ci", "started_at": "2026-05-18T20:56:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:56:12Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291523, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark01-ci", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:04:23Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291534, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark75-ci", "started_at": "2026-05-18T21:02:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:56:08Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291559, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478772", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:01:16Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291561, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark55-ci", "started_at": "2026-05-18T20:56:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:09:39Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291563, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark75-ci", "started_at": "2026-05-18T21:04:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:01:47Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291566, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark10-ci", "started_at": "2026-05-18T20:58:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:12:22Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291568, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark75-ci", "started_at": "2026-05-18T21:09:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:58:45Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291583, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478775", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:05:55Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291595, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T20:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:03:11Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291600, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark55-ci", "started_at": "2026-05-18T21:01:18Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:02:15Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291601, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:58:18Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291605, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478774", "started_at": "2026-05-18T20:55:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:56:50Z", "conclusion": "failure", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291615, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mc7hd", "started_at": "2026-05-18T20:55:26Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:05:49Z", "conclusion": "success", "created_at": "2026-05-18T20:55:19Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76617291725, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-zxd7g", "started_at": "2026-05-18T20:55:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T21:18:13Z", "conclusion": "failure", "created_at": "2026-05-18T21:18:04Z", "event": "push", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76620979965, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059564621", "run_id": 26059564621, "runner_name": "GitHub Actions 1000478777", "started_at": "2026-05-18T21:18:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:48:37Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "dynamic", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002159, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059563447", "run_id": 26059563447, "runner_name": "GitHub Actions 1000478742", "started_at": "2026-05-18T20:47:46Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:49:08Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "dynamic", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002160, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059563447", "run_id": 26059563447, "runner_name": "GitHub Actions 1000478741", "started_at": "2026-05-18T20:47:46Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample. (#24463)\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\nexecutable objects, executable cache, FD-backed file helpers,\nregistration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\nhandles, adapter/device requests, buffer mapping, command encoding,\npipeline creation, bind group creation, command-stream execution, cached\nrecordings, and queue.onSubmittedWorkDone() completion delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\ninto block-backed uint32 streams with dynamic/static binding slots and\nautomatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\nWebGPU has no native command or requires stricter alignment than HAL\ncallers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\nSPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\nWGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n`webgpu` package loader, WASI preopen/output setup, and expected\nfailures for blocking-completion cases that the inline host cannot yet\nsatisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\nand validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\nCMake targets and explicit selection of the WebGPU SPIR-V compiler\ntarget.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.", "completed_at": "2026-05-18T20:48:56Z", "conclusion": "success", "created_at": "2026-05-18T20:47:44Z", "event": "dynamic", "head_branch": "main", "head_sha": "d9a3dd15a552cdded3bda4fcfa65f1341d2b5f92", "job_id": 76616002190, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26059563447", "run_id": 26059563447, "runner_name": "GitHub Actions 1000478743", "started_at": "2026-05-18T20:47:46Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on main", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:31:35Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378421, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-67glz", "started_at": "2026-05-18T18:48:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:05:58Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378433, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_tsan / linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-75fjt", "started_at": "2026-05-18T18:48:39Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T22:51:51Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378461, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc / linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478678", "started_at": "2026-05-18T18:41:21Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T23:27:48Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378466, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm / linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478676", "started_at": "2026-05-18T18:41:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:02:09Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378469, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-j99xh", "started_at": "2026-05-18T18:49:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-19T00:41:12Z", "conclusion": "cancelled", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378489, "labels": ["macos-15-intel"], "name": "macos_x64_clang / macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478670", "started_at": "2026-05-18T18:40:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:36:55Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378496, "labels": ["ah-ubuntu_22_04-c7g_4x-50"], "name": "linux_arm64_clang / linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "arm-hosted_i-0d68db97c6f93d424", "started_at": "2026-05-18T18:41:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:19:31Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378499, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-linux-scale-gtvf9-runner-ldwrk", "started_at": "2026-05-18T18:48:31Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:57:01Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378500, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "azure-windows-scale-4lxzk-runner-d76cp", "started_at": "2026-05-18T19:39:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T22:54:35Z", "conclusion": "success", "created_at": "2026-05-18T18:39:53Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76594378548, "labels": ["macos-14"], "name": "macos_arm64_clang / macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053087706", "run_id": 26053087706, "runner_name": "GitHub Actions 1000478675", "started_at": "2026-05-18T18:41:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T20:23:14Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596556936, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:15:29Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:40:40Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557005, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:35:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:58:51Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557056, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-4mv98", "started_at": "2026-05-18T19:52:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:58:12Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557127, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478728", "started_at": "2026-05-18T18:53:56Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:49:51Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557153, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:39:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:11:05Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557209, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:01:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:38:59Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557226, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:32:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:50:52Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557229, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:45:46Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:49:57Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557236, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:43:42Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:37:33Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557237, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:30:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:57:48Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557248, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478730", "started_at": "2026-05-18T18:54:29Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:52:49Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557254, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:42:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:00:18Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557261, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark01-ci", "started_at": "2026-05-18T18:57:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:09:52Z", "conclusion": "failure", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557281, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478727", "started_at": "2026-05-18T18:53:50Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:08:49Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557283, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mv5t6", "started_at": "2026-05-18T19:01:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:07:15Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557317, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:02:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:00:28Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557319, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-fbjs6", "started_at": "2026-05-18T18:59:29Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T20:36:09Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557323, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:34:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:10:55Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557338, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:07:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:01:15Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557340, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478722", "started_at": "2026-05-18T18:52:50Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:14:20Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557348, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T19:03:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:09:57Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557359, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-gjmj5", "started_at": "2026-05-18T19:00:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T18:58:09Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557377, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478731", "started_at": "2026-05-18T18:54:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:15:32Z", "conclusion": "success", "created_at": "2026-05-18T18:52:16Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557386, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:11:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T19:25:36Z", "conclusion": "success", "created_at": "2026-05-18T18:52:17Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76596557392, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:16:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[WebGPU] Add WebGPU HAL driver, WGSL compiler target, CTS, and sample.\n\nThis adds the first end-to-end WebGPU target path for IREE: a compiler\nbackend that emits WGSL executables and a JavaScript-hosted HAL driver\nthat can submit those executables through the browser/Node WebGPU API\nfrom a freestanding wasm32 runtime. Most gaps now exist in\ninfrastructure and hosting applications, with the HAL being largely\ncomplete.\n\nThe important product boundary is that this is a WebGPU driver for the\nWeb platform, not an Emscripten port and not a native Dawn HAL. The C\nruntime owns IREE's HAL object model, synchronization contracts, command\nrecording, executable metadata, and queue ordering. JavaScript owns the\nambient WebGPU objects, Promise completion delivery, and the import\nmodule that maps integer wasm handles to real\nGPUAdapter/GPUDevice/GPUBuffer/GPUQueue objects.\n\nThat split keeps the ABI narrow. All values crossing the wasm boundary\nare integers or pointers into wasm linear memory. WebGPU objects are\nrepresented as uint32 handles in a JS-side table, handle 0 is null, and\nasync WebGPU APIs complete through the JS proactor token ring introduced\nby the wasm runtime commit. The C side never gets a raw JS object and\nthe JS side does not need to understand HAL resources beyond the\ndeclared import ABI.\n\nThe driver uses an instruction-stream bridge instead of one wasm import\nper HAL command. HAL command buffers and one-shot queue operations\ncompile into compact uint32 instruction blocks. JavaScript walks those\nblocks in one bridge call, resolves dynamic bindings from a binding\ntable, reuses static bindings for cached recordings, batches encoder\ncommands, and submits pending GPUCommandBuffers at explicit\nqueue-surface boundaries. This makes the wasm/JS boundary a\ncommand-stream boundary instead of a per-command overhead cliff.\n\nThe runtime queue contract follows WebGPU's actual execution model.\nCPU-only operations can signal after their wait completes. GPU-submit\noperations wait, encode/submit work, register\nqueue.onSubmittedWorkDone(), and signal HAL semaphores only when WebGPU\nreports that submitted work is complete. Queue epochs and async\nfrontiers preserve causal ordering for downstream waits, while\nsubmitted-provenance tracking keeps FIFO waits from adding unnecessary\nhost-side round trips.\n\nWebGPU does not provide every primitive that IREE's HAL exposes\ndirectly. The driver internalizes those gaps instead of pushing them\nonto callers: fill uses a builtin WGSL compute shader, unaligned\ncopy/update paths fall back to a copy shader, executable loading creates\ncompute pipelines and bind group layouts from WGSL, and command\nexecution presents the usual HAL fill/copy/update/dispatch surface even\nthough WebGPU splits those operations across queue, encoder, and\ncompute-pass APIs.\n\nThe compiler side lowers through the existing SPIR-V path and translates\nSPIR-V to WGSL with Tint/Dawn. The serialized executable format is\n`webgpu-wgsl-fb`: a FlatBuffer containing WGSL shader modules plus\nper-export metadata such as entry point names, workgroup sizes, binding\nflags, constant counts, source/debug data, and the information the\nruntime needs to create pipelines and bind groups. The target is\nregistered as the `webgpu` device and `webgpu-spirv` executable backend.\n\nThe initial runtime support contract is intentionally narrow. WebGPU\nexposes one queue per device, so the driver currently routes through a\nsingle queue while keeping queue state isolated enough for future\nqueue[N] shaping. The JavaScript inline host can validate WGSL and run\nCTS-style entry points, but blocking C code cannot make JavaScript\nPromises settle while the same wasm thread is waiting. CTS expected\nfailures document those blocking-completion cases instead of pretending\nthey are implemented.\n\nThis commit includes:\n\n* A `webgpu` HAL driver with driver/device/allocator/buffer/semaphore/\n  executable objects, executable cache, FD-backed file helpers,\n  registration module, and public driver creation API.\n* A C import ABI and JavaScript companion module for WebGPU object\n  handles, adapter/device requests, buffer mapping, command encoding,\n  pipeline creation, bind group creation, command-stream execution,\n  cached recordings, and queue.onSubmittedWorkDone() completion\n  delivery.\n* A compact WebGPU command ISA and builder that records HAL commands\n  into block-backed uint32 streams with dynamic/static binding slots and\n  automatic encoder begin/end insertion.\n* Builtin WGSL fill/copy shaders used to provide HAL semantics where\n  WebGPU has no native command or requires stricter alignment than HAL\n  callers expose.\n* A `webgpu-spirv` compiler plugin that reuses SPIR-V codegen, prepares\n  SPIR-V for WebGPU constraints, translates with Tint/Dawn, and packages\n  WGSL plus executable metadata into `webgpu-wgsl-fb`.\n* HAL CTS wiring for the wasm32-wasi WebGPU path, including the Node\n  `webgpu` package loader, WASI preopen/output setup, and expected\n  failures for blocking-completion cases that the inline host cannot yet\n  satisfy.\n* A WebGPU hello-world sample that builds a VMFB, dumps generated WGSL,\n  and validates that WGSL through Dawn's WebGPU implementation.\n* Build-system integration for Bazel and CMake, including generated\n  CMake targets and explicit selection of the WebGPU SPIR-V compiler\n  target.\n\nThe CTS coverage exercises the runtime side under wasm32-wasi with the\nJS WebGPU bridge. The passing coverage includes buffer, command buffer,\ncore, file, and queue CTS groups, with expected failures kept to the\noperations that require a blocking C wait while JavaScript Promise\ncompletions are still pending on the same inline host.\n\nTogether with the wasm runtime commit below it, this establishes the\nfirst coherent WebGPU bring-up slice: IREE can generate WGSL for WebGPU,\npackage it in a HAL executable format, create WebGPU pipelines from that\nexecutable, validate a hello-world shader end to end, and run meaningful\nHAL CTS coverage through the same wasm/JS bridge that applications will\nuse. Future changes will build tooling and samples that run VMFB\nprograms.\n\nci-extra: all", "completed_at": "2026-05-18T20:23:21Z", "conclusion": "failure", "created_at": "2026-05-18T20:23:14Z", "event": "pull_request", "head_branch": "users/benvanik/webgpu-hal", "head_sha": "d8b1945ce47220a70d5a6fad6f09af34b97a05a9", "job_id": 76611848126, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053078643", "run_id": 26053078643, "runner_name": "GitHub Actions 1000478738", "started_at": "2026-05-18T20:23:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:58:49Z", "conclusion": "success", "created_at": "2026-05-18T18:51:25Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404222, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-lcmlv", "started_at": "2026-05-18T19:52:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:00:28Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404252, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478714", "started_at": "2026-05-18T18:51:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:42:29Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404260, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:33:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T20:15:27Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404289, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:07:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:08:23Z", "conclusion": "failure", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404296, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478719", "started_at": "2026-05-18T18:51:56Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:18:33Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404326, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:09:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:45:44Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404327, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:40:42Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:00:56Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404347, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-twb5r", "started_at": "2026-05-18T18:58:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:27:28Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404353, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T19:14:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:43:40Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404374, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:37:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:19:54Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404385, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:15:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:02:33Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404394, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:58:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:30:44Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404415, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:25:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:02:43Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404436, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-xxg82", "started_at": "2026-05-18T18:53:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:26:08Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404439, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:15:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:25:16Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404448, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:19:56Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:05:39Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404471, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-p2v97", "started_at": "2026-05-18T18:57:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:55:39Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404472, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:50:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:01:37Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404474, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark75-ci", "started_at": "2026-05-18T18:52:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:16:06Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404494, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:10:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T20:04:12Z", "conclusion": "success", "created_at": "2026-05-18T18:51:26Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76596404665, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:52:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T20:15:36Z", "conclusion": "failure", "created_at": "2026-05-18T20:15:28Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76610587428, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053063193", "run_id": 26053063193, "runner_name": "GitHub Actions 1000478737", "started_at": "2026-05-18T20:15:30Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:17:56Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271468, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-m2nvm", "started_at": "2026-05-18T18:41:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:12:29Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271536, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-windows-scale-4lxzk-runner-dtrsg", "started_at": "2026-05-18T18:55:31Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T18:59:19Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271556, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-wn8gf", "started_at": "2026-05-18T18:41:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:10:05Z", "conclusion": "success", "created_at": "2026-05-18T18:39:16Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76594271604, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug / linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "azure-linux-scale-gtvf9-runner-8b7vt", "started_at": "2026-05-18T18:43:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Vulkan] Refinement: planning, feature discovery, and device contracts (#24505)\n\nThis PR is the first cleanup and capability-enablement pass after the\nVulkan HAL rewrite landed. The rewrite made the driver functional, fast,\nand aligned with the modern HAL queue model; this follow-up turns\nseveral of the remaining large in-file policy clusters into named units\nwith explicit contracts, and adds feature discovery needed for the next\nwave of Vulkan/SPIR-V targets.\n\nThe main design change is a cleaner split between **planning** and\n**acting**: the driver now computes physical-device selection,\nlogical-device options, queue assignment, feature/extension enablement,\nand external-device validation\nbefore the live logical device is assembled. That makes the Vulkan HAL\neasier to review, easier to test without a real GPU, and easier to\nextend as Vulkan feature negotiation grows.\n\nThe PR also sharpens the capability model. Vulkan support is now\ndetermined from enumerated extensions, queried features, and queried\nproperties. Function pointer availability is not used as a feature gate:\nin static-loader or validation-layer configurations a symbol can exist\nwithout the feature being legal to call, so feature availability must\ncome from the Vulkan device model itself.\n\n## What changed\n\n### Logical device creation is now plan-driven\n\nDriver-owned creation and external `VkDevice` wrapping now share the\nsame logical-device planning path. The new `device_plan` unit computes\nthe durable facts needed by the logical device:\n\n- queue role assignment for compute, transfer, and sparse binding;\n- enabled HAL feature bits;\n- enabled Vulkan device extension bits and extension-name arrays;\n- enabled dispatch ABIs;\n- `VkDeviceQueueCreateInfo` storage;\n- the chained `VkPhysicalDeviceFeatures2` feature structs used for\ndriver-owned `vkCreateDevice`;\n- validation of externally supplied device features, extensions, and\nqueues.\n\nThis makes `logical_device.c` more of an apply step: consume a selected\nphysical-device snapshot, consume a device plan, create or wrap the\nlogical device, initialize queues, allocators, executable caches,\nbuiltins, profiling, and file support.\n\n### Device option policy is isolated\n\nDevice option defaults, string-pair parsing, dispatch ABI parsing, and\noption validation now live in `device_options`. That pulls policy out of\nlogical-device construction and gives options focused coverage.\n\nThis also makes the dispatch ABI contract more obvious:\n\n- `descriptor` remains the portable baseline executable ABI.\n- `bda` is the optimized buffer-device-address ABI.\n- `all` enables every supported ABI and lets the runtime select the best\nmatch.\n\n### Physical-device selection is isolated\n\nDriver-owned physical-device selection now lives in\n`physical_device_selection`. It has a small selector API with default,\nid, and path modes.\n\nThe default selector only chooses devices satisfying the Vulkan HAL\nbaseline. Explicit id/path selectors distinguish \u201cnot found\u201d from \u201cfound\nbut unsupported\u201d so diagnostics are cleaner when a user asks for a\nspecific physical device.\n\n### Request flags are no longer mixed with feature bits\n\nThe public Vulkan feature mask previously carried some behavior that is\nnot a Vulkan device feature. This PR splits those concepts:\n\n- device features describe Vulkan features enabled on or supplied by the\nlogical device;\n- request flags describe non-feature behavior such as validation layers,\ndebug utils, object names, and command labels.\n\nThat matters for external wrapping: a wrapped `VkDevice` can now\nexplicitly say which Vulkan device features and extensions are enabled,\nseparately from which debug/profiling behaviors IREE may use.\n\n### Queue affinity resolves to queue lanes\n\nPublic queue-affinity masks are now resolved to concrete queue lanes at\nlogical device ingress. The plan records selected Vulkan queue\nfamily/index pairs for compute, transfer, and sparse binding roles, and\nthe logical device exposes a small queue-lane model over those selected\nqueues.\n\nThis keeps today\u2019s affinity API working while moving the internals\ncloser to the future HAL direction where queue affinity becomes an\nopaque queue handle. The important part is that queue selection is no\nlonger an ad hoc mask test at every callsite: command buffers and\nsubmissions are normalized against the live lane model before work\nreaches the queue.\n\n### Logical-device state has clearer representation boundaries\n\nThe logical device now groups queue state and profiling state into named\naggregates instead of spreading related fields through the whole object:\n\n- queue selections, queue handles, mutexes, queue lanes, and public\naffinity metadata live together;\n- profiling recorder state, calibrated timestamp state, counters, and\nclock alignment live together.\n\nThese are not flashy changes, but they make the big file materially\neasier to reason about. Queue topology and profiling lifetime are now\nvisible as representation boundaries instead of being implicit in field\nordering.\n\n### Debug utils are a real subsystem\n\n`VK_EXT_debug_utils` handling now has a focused `debug_utils` unit. It\nresolves which debug-utils capabilities are actually enabled for the\nlogical device:\n\n- object names;\n- command labels.\n\nCommand-buffer debug groups are recorded in the reusable HAL command\nbuffer and replayed as Vulkan command labels during native\ncommand-buffer recording when command labels are available. Queue object\nnames are derived from the logical device id, queue role, and selected\nVulkan queue family/index.\n\nThis makes command streams more inspectable in Vulkan\ndebugging/profiling tools without putting debug-utils symbol checks\ndirectly into hot logic.\n\n## Unlocked features\n\n### Cooperative matrix device profiles\n\nThe Vulkan HAL now recognizes `VK_KHR_cooperative_matrix`, queries its\nfeature bit, captures its device properties, and exposes the cooperative\nmatrix property rows through a public query API:\n\n```c\niree_hal_vulkan_device_query_cooperative_matrix_properties(...)\n```\n\nThe property rows preserve Vulkan wire values for component types and\nscopes so the compiler side can compare against SPIR-V/Vulkan enums\nwithout inventing a parallel string schema.\n\nLogical-device planning enables cooperative matrix support when the\nextension and feature are present, validates wrapped devices that claim\nthe feature, and exposes query keys for both coarse support and property\ndetails.\n\n### `storageBuffer16BitAccess`\n\nThe physical-device snapshot now includes the Vulkan 1.1 feature chain,\nand the device plan reports/enables `storageBuffer16BitAccess`. This\ncloses a gap in the scalar-storage feature set: 8-bit storage was\nalready visible through Vulkan 1.2 features, and 16-bit storage now\nfollows the same request/report model.\n\n### `VK_KHR_shader_bfloat16`\n\nThe Vulkan HAL now recognizes `VK_KHR_shader_bfloat16` and\nreports/enables its three feature bits when available:\n\n- `shaderBFloat16Type`;\n- `shaderBFloat16DotProduct`;\n- `shaderBFloat16CooperativeMatrix`.\n\nThe implementation includes a local compatibility definition for\n`VkPhysicalDeviceShaderBfloat16FeaturesKHR` so the runtime can build\nwhile the checked-in Vulkan headers lag the extension. The fallback is\nguarded by the extension macro, not by enum-token preprocessor checks.\n\nExternal device wrapping validates the full contract: if a caller claims\nany shader-bfloat16 feature bit, the wrapped device must also report\n`VK_KHR_shader_bfloat16` in its enabled extension bitmask.\n\n### Raw BDA SPIR-V with the Vulkan memory model\n\nRaw BDA executable verification now accepts:\n\n- `OpMemoryModel PhysicalStorageBuffer64 GLSL450`;\n- `OpMemoryModel PhysicalStorageBuffer64 Vulkan`.\n\nThe Vulkan-memory-model form must also declare `OpCapability\nVulkanMemoryModel`. That keeps the verifier permissive enough for modern\nBDA modules without quietly accepting invalid SPIR-V.\n\n### Feature and extension reporting is more complete\n\n`iree-run-module --dump_devices` and Vulkan device queries now have\nbetter coverage for the modern feature set:\n\n- Vulkan 1.1 `storageBuffer16BitAccess`;\n- Vulkan 1.2/1.3 baseline and scalar shader features;\n- Vulkan memory model and device-scope memory model;\n- cooperative matrix support and property rows;\n- shader bfloat16 features;\n- optional extension opportunities.\n\nThe docs now describe the Vulkan HAL baseline explicitly: Vulkan 1.3, a\ncompute queue, `timelineSemaphore`, `scalarBlockLayout`, and\n`synchronization2`. Optional features remain opportunistic instead of\nbecoming part of the baseline.\n\n## External wrapping contract\n\nThis PR makes external wrapping stricter and more explicit. A caller\nwrapping an externally created `VkDevice` must provide:\n\n- the Vulkan feature bits enabled on the device and usable by IREE;\n- the recognized Vulkan device extension bits enabled on the device;\n- non-feature request flags IREE may use;\n- compute, transfer, and sparse-binding queue sets.\n\nThe wrap path verifies that claimed features are actually reported by\nthe physical-device snapshot and that extension-backed features also\nhave the corresponding enabled extension bit. This avoids the old\nambiguity where a feature-like flag could mean \u201crequested,\u201d \u201cavailable,\u201d\n\u201cenabled,\u201d or \u201cdebug behavior,\u201d depending on the callsite.\n\n## Why this is worth doing now\n\nThe Vulkan HAL rewrite intentionally landed with a few large pieces\nstill in one place so the functional change could land without another\nlayer of churn. Now that the rewrite is in `main`, these follow-up\nchanges put the codebase in a better shape for real feature work:\n\n- feature negotiation has a single plan-time home;\n- external wrapping has a clear contract;\n- queue topology is represented in a way that can evolve toward queue\nhandles;\n- debug utils are isolated and testable;\n- physical-device selection is no longer tangled with logical-device\nassembly;\n- new SPIR-V/Vulkan target features can be added as declarative snapshot\nand plan bits instead of more `logical_device.c` branches.\n\nThe result is not just \u201cmore features.\u201d It is a Vulkan HAL that is\neasier to extend without guessing which of five nearby branches owns a\nparticular Vulkan invariant.\n\n## Test coverage\n\nThis PR adds or expands focused unit coverage for the new boundaries:\n\n- `api_test`\n- `command_buffer_test`\n- `debug_utils_test`\n- `device_options_test`\n- `device_plan_test`\n- `physical_device_selection_test`\n- `spirv_test`\n\nLocal focused ASAN run:\n\n```bash\niree-bazel-test \\\n  //runtime/src/iree/hal/drivers/vulkan:api_test \\\n  //runtime/src/iree/hal/drivers/vulkan:command_buffer_test \\\n  //runtime/src/iree/hal/drivers/vulkan:debug_utils_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_options_test \\\n  //runtime/src/iree/hal/drivers/vulkan:device_plan_test \\\n  //runtime/src/iree/hal/drivers/vulkan:physical_device_selection_test \\\n  //runtime/src/iree/hal/drivers/vulkan:spirv_test \\\n  --config=asan\n```\n\nAdditional coverage during the stack included Vulkan subtree ASAN builds\nand Windows/MSVC builds for the focused Vulkan API, device-plan, and\nSPIR-V tests.", "completed_at": "2026-05-18T19:18:05Z", "conclusion": "success", "created_at": "2026-05-18T19:17:57Z", "event": "push", "head_branch": "main", "head_sha": "3309b3377a23ed0796bd0bcd120b7718153757f0", "job_id": 76600994840, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26053059866", "run_id": 26053059866, "runner_name": "GitHub Actions 1000478732", "started_at": "2026-05-18T19:18:00Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:59:21Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796720, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-rl4qh", "started_at": "2026-05-18T18:57:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:01:01Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796733, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark10-ci", "started_at": "2026-05-18T18:56:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:32:22Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796743, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:25:18Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:58:02Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796767, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478712", "started_at": "2026-05-18T18:49:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:56:03Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796840, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:49:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:04:32Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796876, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:00:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:33:36Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796880, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:28:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:31:18Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796883, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:26:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T18:58:14Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796910, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-fnqdv", "started_at": "2026-05-18T18:48:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:35:22Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796933, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark10-ci", "started_at": "2026-05-18T19:31:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T20:00:44Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796936, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark55-ci", "started_at": "2026-05-18T19:49:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:09:45Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796948, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:04:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:03:47Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796951, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "iree-mi308-1", "started_at": "2026-05-18T18:53:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:09:07Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796952, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:01:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T20:07:37Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796954, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:02:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T20:34:20Z", "conclusion": "success", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595796983, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "shark75-ci", "started_at": "2026-05-18T20:23:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:04:22Z", "conclusion": "failure", "created_at": "2026-05-18T18:48:01Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76595797149, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478711", "started_at": "2026-05-18T18:49:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T20:34:28Z", "conclusion": "failure", "created_at": "2026-05-18T20:34:21Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76613708050, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052396398", "run_id": 26052396398, "runner_name": "GitHub Actions 1000478740", "started_at": "2026-05-18T20:34:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:18:29Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018903, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-nwsd8", "started_at": "2026-05-18T18:35:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:01:39Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018947, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-7fdnc", "started_at": "2026-05-18T18:40:30Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:04:47Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018963, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-7drz6", "started_at": "2026-05-18T18:35:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:08:39Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592018996, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-windows-scale-4lxzk-runner-s9ds7", "started_at": "2026-05-18T18:41:34Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:07:16Z", "conclusion": "success", "created_at": "2026-05-18T18:26:16Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76592019071, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "azure-linux-scale-gtvf9-runner-pbj6f", "started_at": "2026-05-18T18:40:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "[Torch] Drop flex attention ordering barrier\n\nRemove the canonicalizer barrier that was only used to force module ordering between flex attention callbacks and their users. Also drop the torch-to-iree test that covered that artificial ordering case.\n\nCo-authored-by: OpenAI Codex <noreply@openai.com>\nSigned-off-by: Keshav Vinayak Jha <keshavvinayakjha@gmail.com>", "completed_at": "2026-05-18T19:18:34Z", "conclusion": "success", "created_at": "2026-05-18T19:18:30Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/scalarize-online-attention-mask-region", "head_sha": "5415ead932e823e1a9212b7007ce55f220521a6d", "job_id": 76601085026, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26052394472", "run_id": 26052394472, "runner_name": "GitHub Actions 1000478733", "started_at": "2026-05-18T19:18:32Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T19:02:36Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460504, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-hg2zx", "started_at": "2026-05-18T18:34:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T19:26:55Z", "conclusion": "success", "created_at": "2026-05-18T18:05:35Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76588460708, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "azure-linux-scale-gtvf9-runner-vppzk", "started_at": "2026-05-18T18:26:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T19:27:03Z", "conclusion": "success", "created_at": "2026-05-18T19:26:56Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76602515281, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051343839", "run_id": 26051343839, "runner_name": "GitHub Actions 1000478734", "started_at": "2026-05-18T19:26:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:58:00Z", "conclusion": "failure", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869498, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478689", "started_at": "2026-05-18T18:43:00Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T18:58:28Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869600, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark55-ci", "started_at": "2026-05-18T18:51:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T19:15:19Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869767, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:09:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T19:28:05Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869827, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark01-ci", "started_at": "2026-05-18T19:18:35Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T20:02:02Z", "conclusion": "success", "created_at": "2026-05-18T18:42:43Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76594869844, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "shark75-ci", "started_at": "2026-05-18T19:56:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-05-19T01:20:10.050741Z", "commit_message": "Merge remote-tracking branch 'origin/main' into pr23778-repair-no24408", "completed_at": "2026-05-18T20:02:11Z", "conclusion": "failure", "created_at": "2026-05-18T20:02:02Z", "event": "pull_request", "head_branch": "users/keshavvinayak01/gpuvectoralloc-swizzle", "head_sha": "5515b1b02398e2e43f4a6970a10625ee151f13c8", "job_id": 76608398666, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26051354042", "run_id": 26051354042, "runner_name": "GitHub Actions 1000478736", "started_at": "2026-05-18T20:02:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
