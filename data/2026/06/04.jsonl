{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:35:21Z", "conclusion": "success", "created_at": "2026-06-04T06:35:10Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463251509, "labels": ["ubuntu-24.04"], "name": "setup_metadata", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486158", "started_at": "2026-06-04T06:35:12Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:41:57Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277144, "labels": ["macos-14"], "name": "macos :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486160", "started_at": "2026-06-04T06:35:23Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277145, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486161", "started_at": "2026-06-04T06:35:23Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277161, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486165", "started_at": "2026-06-04T06:35:27Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277165, "labels": ["macos-14"], "name": "macos :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486159", "started_at": "2026-06-04T06:35:24Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:37:42Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277180, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-tf-compiler-tools-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486168", "started_at": "2026-06-04T06:35:24Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:53:03Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277185, "labels": ["windows-2022"], "name": "windows :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486162", "started_at": "2026-06-04T06:35:24Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277188, "labels": ["windows-2022"], "name": "windows :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486163", "started_at": "2026-06-04T06:35:24Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:49:56Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277202, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486164", "started_at": "2026-06-04T06:35:28Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277206, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486169", "started_at": "2026-06-04T06:35:24Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277226, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486166", "started_at": "2026-06-04T06:35:26Z", "status": "in_progress", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T07:07:33Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277291, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-runtime-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486167", "started_at": "2026-06-04T06:35:25Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T06:35:10Z", "conclusion": "success", "created_at": "2026-06-04T06:34:29Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463161859, "labels": ["ubuntu-24.04"], "name": "Tag candidate release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935194498", "run_id": 26935194498, "runner_name": "GitHub Actions 1000486157", "started_at": "2026-06-04T06:34:31Z", "status": "completed", "workflow_id": 17505351, "workflow_name": "Schedule candidate release", "workflow_path": ".github/workflows/schedule_candidate_release.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T05:55:54Z", "conclusion": "failure", "created_at": "2026-06-04T05:46:30Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79457641566, "labels": ["ubuntu-24.04"], "name": "colab", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26933406591", "run_id": 26933406591, "runner_name": "GitHub Actions 1000486153", "started_at": "2026-06-04T05:46:32Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T05:51:04Z", "conclusion": "success", "created_at": "2026-06-04T05:46:30Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79457641603, "labels": ["ubuntu-24.04"], "name": "samples", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26933406591", "run_id": 26933406591, "runner_name": "GitHub Actions 1000486154", "started_at": "2026-06-04T05:46:32Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-06-04T07:57:33.106740Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T05:56:03Z", "conclusion": "failure", "created_at": "2026-06-04T05:55:55Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79458665926, "labels": ["ubuntu-24.04"], "name": "samples_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26933406591", "run_id": 26933406591, "runner_name": "GitHub Actions 1000486155", "started_at": "2026-06-04T05:55:57Z", "status": "completed", "workflow_id": 42531193, "workflow_name": "samples", "workflow_path": ".github/workflows/samples.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:46:11Z", "conclusion": "success", "created_at": "2026-06-04T11:45:02Z", "event": "dynamic", "head_branch": "latest-snapshot", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511415225, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949665050", "run_id": 26949665050, "runner_name": "GitHub Actions 1000486187", "started_at": "2026-06-04T11:45:04Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on latest-snapshot", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:46:21Z", "conclusion": "success", "created_at": "2026-06-04T11:45:02Z", "event": "dynamic", "head_branch": "latest-snapshot", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511415249, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949665050", "run_id": 26949665050, "runner_name": "GitHub Actions 1000486189", "started_at": "2026-06-04T11:45:05Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on latest-snapshot", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:45:54Z", "conclusion": "success", "created_at": "2026-06-04T11:45:02Z", "event": "dynamic", "head_branch": "latest-snapshot", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511415263, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949665050", "run_id": 26949665050, "runner_name": "GitHub Actions 1000486188", "started_at": "2026-06-04T11:45:04Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on latest-snapshot", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:46:52Z", "conclusion": "failure", "created_at": "2026-06-04T11:44:24Z", "event": "release", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511321449, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949639584", "run_id": 26949639584, "runner_name": "GitHub Actions 1000486186", "started_at": "2026-06-04T11:44:27Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:44:16Z", "conclusion": "success", "created_at": "2026-06-04T11:42:20Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511005394, "labels": ["ubuntu-24.04"], "name": "Validate packages", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949544597", "run_id": 26949544597, "runner_name": "GitHub Actions 1000486184", "started_at": "2026-06-04T11:42:23Z", "status": "completed", "workflow_id": 12159822, "workflow_name": "Validate and Publish Release", "workflow_path": ".github/workflows/validate_and_publish_release.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:45:00Z", "conclusion": "success", "created_at": "2026-06-04T11:44:17Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79511301589, "labels": ["ubuntu-24.04"], "name": "Publish release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26949544597", "run_id": 26949544597, "runner_name": "GitHub Actions 1000486185", "started_at": "2026-06-04T11:44:19Z", "status": "completed", "workflow_id": 12159822, "workflow_name": "Validate and Publish Release", "workflow_path": ".github/workflows/validate_and_publish_release.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T10:28:39Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499604983, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946179588", "run_id": 26946179588, "runner_name": "GitHub Actions 1000486182", "started_at": "2026-06-04T10:28:40Z", "status": "in_progress", "workflow_id": 109041211, "workflow_name": "CI - Linux x64 gcc", "workflow_path": ".github/workflows/ci_linux_x64_gcc.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:24:10Z", "conclusion": "success", "created_at": "2026-06-04T10:28:33Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499591877, "labels": ["ah-ubuntu_22_04-c7g_4x-50"], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946175694", "run_id": 26946175694, "runner_name": "arm-hosted_i-0f9e8013cc75219a9", "started_at": "2026-06-04T10:29:58Z", "status": "completed", "workflow_id": 108693130, "workflow_name": "CI - Linux arm64 clang", "workflow_path": ".github/workflows/ci_linux_arm64_clang.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T10:28:29Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499580716, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946172053", "run_id": 26946172053, "runner_name": "GitHub Actions 1000486181", "started_at": "2026-06-04T10:28:30Z", "status": "in_progress", "workflow_id": 109041212, "workflow_name": "CI - Linux x64 clang BYO LLVM", "workflow_path": ".github/workflows/ci_linux_x64_clang_byollvm.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T10:47:00Z", "conclusion": "success", "created_at": "2026-06-04T10:28:27Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499576084, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946170748", "run_id": 26946170748, "runner_name": "azure-linux-scale-gtvf9-runner-cl7nj", "started_at": "2026-06-04T10:28:28Z", "status": "completed", "workflow_id": 109054114, "workflow_name": "CI - Linux x64 clang TSan", "workflow_path": ".github/workflows/ci_linux_x64_clang_tsan.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T13:00:25Z", "conclusion": "success", "created_at": "2026-06-04T10:28:16Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499547725, "labels": ["macos-14"], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946162366", "run_id": 26946162366, "runner_name": "GitHub Actions 1000486180", "started_at": "2026-06-04T10:28:18Z", "status": "completed", "workflow_id": 137427856, "workflow_name": "CI - macOS arm64 clang", "workflow_path": ".github/workflows/ci_macos_arm64_clang.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": null, "conclusion": null, "created_at": "2026-06-04T10:28:13Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499538990, "labels": ["macos-15-intel"], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946159967", "run_id": 26946159967, "runner_name": "GitHub Actions 1000486179", "started_at": "2026-06-04T10:28:15Z", "status": "in_progress", "workflow_id": 109041301, "workflow_name": "CI - macOS x64 clang", "workflow_path": ".github/workflows/ci_macos_x64_clang.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T10:55:55Z", "conclusion": "success", "created_at": "2026-06-04T10:27:57Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499500413, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946148701", "run_id": 26946148701, "runner_name": "azure-linux-scale-gtvf9-runner-dnsck", "started_at": "2026-06-04T10:27:59Z", "status": "completed", "workflow_id": 109041213, "workflow_name": "CI - Linux x64 clang debug", "workflow_path": ".github/workflows/ci_linux_x64_clang_debug.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:42:11Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277145, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486161", "started_at": "2026-06-04T06:35:23Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T09:17:26Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277161, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build main-dist-linux Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486165", "started_at": "2026-06-04T06:35:27Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T09:08:17Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277165, "labels": ["macos-14"], "name": "macos :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486159", "started_at": "2026-06-04T06:35:24Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T09:51:43Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277188, "labels": ["windows-2022"], "name": "windows :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486163", "started_at": "2026-06-04T06:35:24Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:32:32Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277206, "labels": ["ubuntu-24.04"], "name": "linux-x86_64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486169", "started_at": "2026-06-04T06:35:24Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T09:08:35Z", "conclusion": "success", "created_at": "2026-06-04T06:35:21Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79463277226, "labels": ["ubuntu-24.04-arm"], "name": "linux-aarch64 :: Build py-compiler-pkg Package", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486166", "started_at": "2026-06-04T06:35:26Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T13:28:52.233775Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T11:42:19Z", "conclusion": "success", "created_at": "2026-06-04T11:42:12Z", "event": "workflow_dispatch", "head_branch": "iree-3.12.0rc20260604", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79510982547, "labels": ["ubuntu-24.04"], "name": "Trigger validate and publish release", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26935221744", "run_id": 26935221744, "runner_name": "GitHub Actions 1000486183", "started_at": "2026-06-04T11:42:14Z", "status": "completed", "workflow_id": 4194019, "workflow_name": "Build Release Packages", "workflow_path": ".github/workflows/build_package.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:07:14Z", "conclusion": "success", "created_at": "2026-06-04T15:05:51Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548905661, "labels": ["ubuntu-latest"], "name": "Analyze (javascript)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960409479", "run_id": 26960409479, "runner_name": "GitHub Actions 1000486376", "started_at": "2026-06-04T15:05:53Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:06:24Z", "conclusion": "failure", "created_at": "2026-06-04T15:05:51Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548905759, "labels": ["ubuntu-latest"], "name": "Analyze (actions)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960409479", "run_id": 26960409479, "runner_name": "GitHub Actions 1000486378", "started_at": "2026-06-04T15:05:54Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:06:24Z", "conclusion": "failure", "created_at": "2026-06-04T15:05:51Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548905793, "labels": ["ubuntu-latest"], "name": "Analyze (python)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960409479", "run_id": 26960409479, "runner_name": "GitHub Actions 1000486375", "started_at": "2026-06-04T15:05:53Z", "status": "completed", "workflow_id": 65776553, "workflow_name": "Push on gh-pages", "workflow_path": "dynamic/github-code-scanning/codeql"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:06:03Z", "conclusion": "success", "created_at": "2026-06-04T15:05:51Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548906134, "labels": ["ubuntu-latest"], "name": "build", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960408680", "run_id": 26960408680, "runner_name": "GitHub Actions 1000486377", "started_at": "2026-06-04T15:05:53Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:06:34Z", "conclusion": "success", "created_at": "2026-06-04T15:06:04Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548955409, "labels": ["ubuntu-latest"], "name": "deploy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960408680", "run_id": 26960408680, "runner_name": "GitHub Actions 1000486380", "started_at": "2026-06-04T15:06:07Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "Deployed 4bced69 with MkDocs version: 1.6.1", "completed_at": "2026-06-04T15:06:10Z", "conclusion": "success", "created_at": "2026-06-04T15:06:04Z", "event": "dynamic", "head_branch": "gh-pages", "head_sha": "2139344fb386454b6edcba190df355a5b82deb7f", "job_id": 79548955450, "labels": ["ubuntu-latest"], "name": "report-build-status", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960408680", "run_id": 26960408680, "runner_name": "GitHub Actions 1000486379", "started_at": "2026-06-04T15:06:06Z", "status": "completed", "workflow_id": 16770311, "workflow_name": "pages build and deployment", "workflow_path": "dynamic/pages/pages-build-deployment"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T15:05:50Z", "conclusion": "success", "created_at": "2026-06-04T14:59:43Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79547568339, "labels": ["ubuntu-24.04"], "name": "publish_website", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26960032755", "run_id": 26960032755, "runner_name": "GitHub Actions 1000486374", "started_at": "2026-06-04T14:59:46Z", "status": "completed", "workflow_id": 8864285, "workflow_name": "Publish Website", "workflow_path": ".github/workflows/publish_website.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "success", "created_at": "2026-06-04T13:55:26Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534261313, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": "GitHub Actions 1000486257", "started_at": "2026-06-04T14:01:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535624964, "labels": [], "name": "Build Packages", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535624980, "labels": [], "name": "Test AMD MI355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625168, "labels": [], "name": "Test AMD R9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625169, "labels": [], "name": "Test Torch", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:01Z", "conclusion": "success", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625198, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": "GitHub Actions 1000486340", "started_at": "2026-06-04T14:23:58Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625467, "labels": [], "name": "Test TensorFlow", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625473, "labels": [], "name": "Test AMD W7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625476, "labels": [], "name": "Test ONNX", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625643, "labels": [], "name": "Test RISC-V 64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625684, "labels": [], "name": "Test PJRT plugin", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625734, "labels": [], "name": "Test Sharktank", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625852, "labels": [], "name": "Unit Test", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535625897, "labels": [], "name": "Test Android", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:03Z", "conclusion": "skipped", "created_at": "2026-06-04T14:02:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79535626463, "labels": [], "name": "Test AMD MI325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284813", "run_id": 26956284813, "runner_name": null, "started_at": "2026-06-04T14:02:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "success", "created_at": "2026-06-04T13:55:25Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534260830, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": "GitHub Actions 1000486230", "started_at": "2026-06-04T13:55:53Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384072, "labels": [], "name": "linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384257, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384299, "labels": [], "name": "linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384324, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384423, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384438, "labels": [], "name": "linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384447, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384508, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384536, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384537, "labels": [], "name": "runtime :: ${{ matrix.name }}", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384548, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384565, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:02Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384693, "labels": [], "name": "runtime_tracing :: ${{ matrix.runs-on }} :: ${{ matrix.provider }}", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384775, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384818, "labels": [], "name": "windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:02Z", "conclusion": "skipped", "created_at": "2026-06-04T13:56:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534384890, "labels": [], "name": "linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": null, "started_at": "2026-06-04T13:56:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:13Z", "conclusion": "success", "created_at": "2026-06-04T13:56:03Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534385256, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284638", "run_id": 26956284638, "runner_name": "GitHub Actions 1000486301", "started_at": "2026-06-04T14:12:10Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Add design doc for the LLVMCPU C-ukernel framework.\n\nAdds README.md for compiler/plugins/target/LLVMCPU/builtins/ukernel/ as a\nsingle final documentation commit, rather than iterating the doc across\nthe earlier framework commits (less churn for review).\n\nCovers: scope (the framework is op-agnostic in principle, with\n`inner_tiled` as the initial and primary case getting the detailed\ntreatment \u2014 inner-K-loop-only contract, and how specialization splits\nbetween symbol selection on the `MMAIntrinsic` and post-inlining loop\nunrolling on the `intrinsics_{m,n,k}` factors); the two structural\npillars (lives in compiler/, bitcode-only; self-contained IR via\n`hal.executable_object`); the end-to-end lowering flow; the\n`--iree-llvmcpu-enable-llvm-ukernels` user flag; bring-your-own-ukernel;\nan authoring guide; testing at both the lit (compiler IR) and\n`tests/e2e/matmul` (numerical) levels; and the three seed examples.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:01:24Z", "conclusion": "success", "created_at": "2026-06-04T13:55:25Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-design-doc", "head_sha": "678d998e7274d3b7be7e47c3d0f0b228c0872416", "job_id": 79534258674, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956284328", "run_id": 26956284328, "runner_name": "GitHub Actions 1000486245", "started_at": "2026-06-04T13:59:15Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:11Z", "conclusion": "success", "created_at": "2026-06-04T13:55:24Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534255286, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956283219", "run_id": 26956283219, "runner_name": "GitHub Actions 1000486243", "started_at": "2026-06-04T13:58:18Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:51Z", "conclusion": "success", "created_at": "2026-06-04T13:55:23Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534252925, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486224", "started_at": "2026-06-04T13:55:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:54Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349025, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486247", "started_at": "2026-06-04T13:59:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:36Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349097, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486236", "started_at": "2026-06-04T13:56:24Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:30Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349102, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486253", "started_at": "2026-06-04T14:00:50Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:40Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349117, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486309", "started_at": "2026-06-04T14:14:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:27Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349152, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486242", "started_at": "2026-06-04T13:58:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:29:13Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349161, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486364", "started_at": "2026-06-04T14:28:10Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:41Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349203, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "azure-linux-scale-gtvf9-runner-dkf9b", "started_at": "2026-06-04T13:58:12Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:29:44Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349290, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486363", "started_at": "2026-06-04T14:28:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:12Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349299, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486272", "started_at": "2026-06-04T14:05:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:57:58Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349315, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486237", "started_at": "2026-06-04T13:56:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:37Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349326, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "azure-windows-scale-4lxzk-runner-tj7qz", "started_at": "2026-06-04T13:55:53Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:14Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349349, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486238", "started_at": "2026-06-04T13:57:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:03Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349408, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "azure-linux-scale-gtvf9-runner-jl6vz", "started_at": "2026-06-04T13:57:34Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:36Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349422, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486258", "started_at": "2026-06-04T14:02:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:01:51Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349438, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486250", "started_at": "2026-06-04T14:00:29Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:34Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349452, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "azure-linux-scale-gtvf9-runner-mqvhk", "started_at": "2026-06-04T13:57:47Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:43Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349468, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486275", "started_at": "2026-06-04T14:07:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:37:15Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349472, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "azure-linux-scale-gtvf9-runner-bw6xm", "started_at": "2026-06-04T13:58:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:59Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349491, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486262", "started_at": "2026-06-04T14:03:05Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349874, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534349888, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534350005, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534350019, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534350046, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534350094, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534350120, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:37:23Z", "conclusion": "failure", "created_at": "2026-06-04T14:37:15Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79542909808, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282342", "run_id": 26956282342, "runner_name": "GitHub Actions 1000486370", "started_at": "2026-06-04T14:37:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:54Z", "conclusion": "success", "created_at": "2026-06-04T13:55:23Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534253299, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486259", "started_at": "2026-06-04T14:02:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:13:06Z", "conclusion": "success", "created_at": "2026-06-04T14:02:54Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79535796586, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "azure-linux-scale-gtvf9-runner-tzgpb", "started_at": "2026-06-04T14:02:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:13:10Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902411, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark01-ci", "started_at": "2026-06-04T16:08:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:22:52Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902470, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486324", "started_at": "2026-06-04T14:19:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:50:50Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902482, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:47:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:35:08Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902510, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486354", "started_at": "2026-06-04T14:26:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:32Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902522, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486337", "started_at": "2026-06-04T14:23:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:28Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902523, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-t4fxp", "started_at": "2026-06-04T14:20:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:17:21Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902542, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-mmtrv", "started_at": "2026-06-04T14:13:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:44:52Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902552, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:34:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:21:05Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902562, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486325", "started_at": "2026-06-04T14:19:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:22:21Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902567, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:19:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:03:12Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902573, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:34:28Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902576, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-rbqhh", "started_at": "2026-06-04T14:24:46Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:58:53Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902580, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:55:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:06:50Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902586, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:02:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:28:32Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902587, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:25:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:34:29Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902597, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:24:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:30:28Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902607, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:29:00Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:41:22Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902615, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486355", "started_at": "2026-06-04T14:27:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:19:41Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902634, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:16:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:08Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902656, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486339", "started_at": "2026-06-04T14:23:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:36:36Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902662, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:32:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:31:23Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902668, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-l6lb7", "started_at": "2026-06-04T14:30:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:28:35Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902709, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:24:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:26Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902726, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486343", "started_at": "2026-06-04T14:24:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:51:22Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902739, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:40:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:39:48Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902791, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-2w2gb", "started_at": "2026-06-04T14:31:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:56Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902793, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486335", "started_at": "2026-06-04T14:22:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:21Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902821, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486332", "started_at": "2026-06-04T14:22:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:20:38Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902926, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:16:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:52:54Z", "conclusion": "success", "created_at": "2026-06-04T14:13:06Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79537902935, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:45:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:13:15Z", "conclusion": "success", "created_at": "2026-06-04T16:13:11Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79562692469, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956282545", "run_id": 26956282545, "runner_name": "GitHub Actions 1000486387", "started_at": "2026-06-04T16:13:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Fill in the bf16 and i8 ukernel bodies + e2e tests.\n\nReplaces the bf16 and i8-VNNI seeds' stub bodies with real SIMD\nimplementations, both generic over the `intrinsics_{m,n,k}` unrolling\nfactors and structured like the AMDGPU C ukernel they were adapted from\n(`iree_uk_amdgpu_multi_mma_mfma_i32_16x16x32_i8`): accumulators in\nregisters, an outer loop over the K tiles (`k_outer`), and inside it the\n`(intrinsics_m, intrinsics_n, intrinsics_k)` unroll. The `intrinsics_*`\narrive as constants at the inlined call site, so the loops fully unroll\nand the `acc_regs` arrays become fixed register files -- the bitcode-LTO\nequivalent of a C++ template, as the README describes.\n\n  - bf16 (`MMA_X86_AVX512BF16_1x16x2_F32_BF16`): one `_mm512_dpbf16_ps`\n    per (m, n, k), with the LHS K-pair broadcast via `set1_ps`.\n  - i8 (`MMA_X86_AVX512VNNI_16x16x2_I32_I8_CASTI16`): the 16x16x2 tile is\n    bit-compatible with the codegen path `lowerX86Avx512Vnni16x16x2I8` --\n    one `vpmovsxbw` widen of each i8 panel to i16, the `vpshufd` /\n    `vbroadcasti32x4` fan-out, and 16 `vpdpwssd` over the block-interleaved\n    (rlo, chi, rhi, clo) ACC layout. The i8 ukernel needs `-mavx512bw` for\n    the widen, so it is added to the VNNI copts.\n\n`LLVMCPUSelectUKernels` now only selects a ukernel when its bitcode\nactually exists (via `attachUKernelBitcodeOnOp`'s bool return), so an\n`MMAIntrinsic` the cost model picks but for which no seed exists -- e.g.\nthe M<->N-swapped `MMA_X86_AVX512BF16_16x1x2_F32_BF16` -- falls back to\ncodegen instead of dangling an undefined symbol.\n\nAdds two execution/numerical tests, the first of the new C-bitcode\nukernel path: `e2e_matmul_cpu_dt_inner_tiled_llvm_ukernel_bf16_f32`\n(avx512bf16) and `..._i8_i32` (avx512vnni). Each compiles a data-tiled\nmatmul with `--iree-llvmcpu-enable-llvm-ukernels=inner_tiled`, links the\nukernel bitcode, runs on host and checks results against a reference --\nexercising the operand threading and generic `intrinsics_{m,n,k}`\nunrolling that the IR-level lit tests cannot. Both were confirmed to\nactually select their ukernel (not silently fall back to codegen).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:30Z", "conclusion": "success", "created_at": "2026-06-04T13:55:22Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bodies", "head_sha": "8af2850dc5927b0715897a86da0f57887d3dfb65", "job_id": 79534248780, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956281654", "run_id": 26956281654, "runner_name": "GitHub Actions 1000486229", "started_at": "2026-06-04T13:55:53Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:02Z", "conclusion": "success", "created_at": "2026-06-04T13:55:19Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79534240702, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278999", "run_id": 26956278999, "runner_name": "GitHub Actions 1000486241", "started_at": "2026-06-04T13:58:01Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:44Z", "conclusion": "success", "created_at": "2026-06-04T13:55:20Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79534241766, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279129", "run_id": 26956279129, "runner_name": "GitHub Actions 1000486256", "started_at": "2026-06-04T14:01:26Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:21Z", "conclusion": "success", "created_at": "2026-06-04T13:55:19Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79534240726, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486248", "started_at": "2026-06-04T14:00:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:54Z", "conclusion": "success", "created_at": "2026-06-04T14:00:22Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79535267381, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "azure-linux-scale-gtvf9-runner-9xmbn", "started_at": "2026-06-04T14:00:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:15:40Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447786, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486307", "started_at": "2026-06-04T14:13:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:21:13Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447848, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486304", "started_at": "2026-06-04T14:12:48Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:15:05Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447850, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-nl48d", "started_at": "2026-06-04T14:10:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:53Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447871, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-r6qcl", "started_at": "2026-06-04T14:21:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:51Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447917, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486310", "started_at": "2026-06-04T14:14:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:14:50Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447935, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T16:13:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:25Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447941, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486356", "started_at": "2026-06-04T14:27:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:49:48Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537447977, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:43:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:32Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448040, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486322", "started_at": "2026-06-04T14:19:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:47:33Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448067, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:40:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:10:24Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448075, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:08:27Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:35:06Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448152, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mj8bk", "started_at": "2026-06-04T14:27:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:24Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448159, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486333", "started_at": "2026-06-04T14:22:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:12:14Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448166, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:03:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:30:17Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448183, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-pnrjf", "started_at": "2026-06-04T14:29:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:42:46Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448186, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:37:27Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:39Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448189, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486308", "started_at": "2026-06-04T14:14:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:05:24Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448195, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:59:50Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:34:25Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448199, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:24:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:37:26Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448212, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:28:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:08:07Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448214, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T16:02:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:31Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448218, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mgn4k", "started_at": "2026-06-04T14:16:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:18Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448219, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486323", "started_at": "2026-06-04T14:19:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:38:00Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448220, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:29:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:34:24Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448232, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:27:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:55:02Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448243, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:47:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:06:53Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448251, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:02:30Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:34:23Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448320, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:24:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:04Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448381, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486345", "started_at": "2026-06-04T14:24:42Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:11:26Z", "conclusion": "success", "created_at": "2026-06-04T14:10:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79537448591, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:06:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:11:31Z", "conclusion": "success", "created_at": "2026-06-04T16:11:27Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79562352455, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956279028", "run_id": 26956279028, "runner_name": "GitHub Actions 1000486386", "started_at": "2026-06-04T16:11:29Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "success", "created_at": "2026-06-04T13:55:20Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79534241352, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486319", "started_at": "2026-06-04T14:18:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:18Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095062, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486357", "started_at": "2026-06-04T14:27:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:32Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095106, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486348", "started_at": "2026-06-04T14:25:27Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:32:36Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095131, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486359", "started_at": "2026-06-04T14:27:25Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:09Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095153, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486346", "started_at": "2026-06-04T14:25:19Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:05Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095240, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486349", "started_at": "2026-06-04T14:25:37Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:36:08Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095244, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486358", "started_at": "2026-06-04T14:27:12Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:38:09Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095306, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486347", "started_at": "2026-06-04T14:25:22Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:52Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095362, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486360", "started_at": "2026-06-04T14:27:32Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:26:04Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095364, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486341", "started_at": "2026-06-04T14:24:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:22Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095374, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486350", "started_at": "2026-06-04T14:25:53Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:37:34Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095421, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486351", "started_at": "2026-06-04T14:25:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:31:59Z", "conclusion": "failure", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095479, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "azure-linux-scale-gtvf9-runner-jtzzp", "started_at": "2026-06-04T14:19:00Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:10Z", "conclusion": "failure", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095518, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "azure-linux-scale-gtvf9-runner-mkf9f", "started_at": "2026-06-04T14:19:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:52:45Z", "conclusion": "failure", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095520, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "azure-linux-scale-gtvf9-runner-hk2rd", "started_at": "2026-06-04T14:18:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:37:24Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095534, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486352", "started_at": "2026-06-04T14:26:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:30:32Z", "conclusion": "failure", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095559, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "azure-windows-scale-4lxzk-runner-gfgf4", "started_at": "2026-06-04T14:20:40Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:30:00Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095560, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486362", "started_at": "2026-06-04T14:27:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:55Z", "conclusion": "success", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095743, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486353", "started_at": "2026-06-04T14:26:23Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:06Z", "conclusion": "failure", "created_at": "2026-06-04T14:18:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095787, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "azure-linux-scale-gtvf9-runner-t5j7c", "started_at": "2026-06-04T14:18:53Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095893, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095894, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095930, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539095997, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539096452, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539096461, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:51Z", "conclusion": "skipped", "created_at": "2026-06-04T14:18:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79539096958, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": null, "started_at": "2026-06-04T14:18:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] End-to-end-through-codegen lit test for ukernel framework.\n\nCloses the loop on the new C-bitcode ukernel framework with an\n`iree-opt` lit test that drives the full LLVMCPU codegen pipeline on an\n`inner_tiled` rooted dispatch wrapped in the standard\n`hal.executable.variant` + binding-subspan structure, and asserts the\nwhole chain end-to-end:\n\n  - Bitcode reaches the dispatch's `hal.executable.variant` as a\n    first-class `objects(...)` attribute (the long-lived home).\n  - `llvm.func @iree_uk_mma_\u2026` is declared with `hal.import.bitcode =\n    true`, the flag that tells `RewriteExternCallOpToDynamicImportCallOp`\n    to skip the runtime-import-table indirection.\n  - A direct `llvm.call @iree_uk_mma_\u2026` replaces the original\n    `inner_tiled` \u2014 no `__import_ordinal_iree_uk_mma_\u2026` (which would mean\n    we accidentally re-used the legacy runtime-resolved path), no\n    leftover `inner_tiled`, no leftover `ukernel.generic`.\n\nTo make that chain work, two pieces had to land alongside the test:\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now actually\n    builds the `UKernelGenericOp` (returning `success`, not `nullopt`),\n    setting `fn_def_attrs = {hal.import.bitcode = true}` on it. That\n    flag propagates onto the `func.func` declaration that\n    `LowerUKernelOpsToCalls` synthesizes, and through it onto the\n    `llvm.func` that `ConvertToLLVM` produces. Without this, the\n    \"self-contained bitcode\" property of the new framework would not\n    hold \u2014 every external call would be routed through the runtime\n    import table just like the legacy mmt4d ukernels.\n\n  * Bitcode lookup-and-attach is factored into a public helper\n    `IREE::CPU::attachUKernelBitcodeOnOp`, called both from\n    `LLVMCPUSelectUKernels` (at kernel-config time, mirroring GPU's\n    `ensureUKernelBitcodeAndFinalizeConfig`) and from the provider\n    itself (as a backstop for the BYO / hand-written-IR paths). It\n    attaches the bitcode both as a `hal.executable.objects` discardable\n    attribute on the op (the GPU-aligned shape) *and* directly on the\n    enclosing `hal.executable.variant`'s `objects` operand attribute\n    (the durable home, sidestepping passes that strip discardable\n    attrs through `ConvertToLLVM`).\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:52:51Z", "conclusion": "failure", "created_at": "2026-06-04T14:52:45Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline-test", "head_sha": "0907e8511dfa1bc897c1c5f676ee413023b1473d", "job_id": 79546113282, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956278990", "run_id": 26956278990, "runner_name": "GitHub Actions 1000486373", "started_at": "2026-06-04T14:52:48Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:41Z", "conclusion": "success", "created_at": "2026-06-04T13:55:15Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534225966, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274262", "run_id": 26956274262, "runner_name": "GitHub Actions 1000486249", "started_at": "2026-06-04T14:00:23Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:13Z", "conclusion": "success", "created_at": "2026-06-04T13:55:15Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534226591, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486231", "started_at": "2026-06-04T13:56:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:57Z", "conclusion": "success", "created_at": "2026-06-04T13:56:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534420010, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "azure-linux-scale-gtvf9-runner-qwzps", "started_at": "2026-06-04T13:58:18Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:09:25Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429654, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-kfgng", "started_at": "2026-06-04T14:07:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:21Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429665, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486317", "started_at": "2026-06-04T14:17:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:09:44Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429684, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-bpp9l", "started_at": "2026-06-04T14:06:00Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:55:18Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429693, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:44:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:40:48Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429695, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:36:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:20Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429708, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-wffwx", "started_at": "2026-06-04T14:09:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:12:33Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429709, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T16:09:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:38Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429713, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486291", "started_at": "2026-06-04T14:10:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:16:02Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429720, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T16:12:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:45:18Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429721, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:34:25Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:55:18Z", "conclusion": "failure", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429723, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:50:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:16Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429746, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486278", "started_at": "2026-06-04T14:07:13Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:09:34Z", "conclusion": "failure", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429752, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:40:49Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:16:58Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429773, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:10:26Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:56Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429791, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486329", "started_at": "2026-06-04T14:21:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:20:32Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429798, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:19:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:32:31Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429800, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-2hw2f", "started_at": "2026-06-04T14:24:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:57Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429805, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486321", "started_at": "2026-06-04T14:18:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:56Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429824, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486277", "started_at": "2026-06-04T14:07:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:14:53Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429829, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:11:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:41Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429833, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486292", "started_at": "2026-06-04T14:10:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:38Z", "conclusion": "failure", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429855, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-l9pxf", "started_at": "2026-06-04T14:23:37Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:22:07Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429892, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:20:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:58Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429896, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:07:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:03:21Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429911, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:00:07Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:22:46Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429918, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486311", "started_at": "2026-06-04T14:14:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:24:05Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429924, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:20:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:05Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429983, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486302", "started_at": "2026-06-04T14:12:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:51:39Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536429986, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:47:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:24:06Z", "conclusion": "success", "created_at": "2026-06-04T14:05:58Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79536430012, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:15:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:22:11Z", "conclusion": "failure", "created_at": "2026-06-04T16:22:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79564409161, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956274441", "run_id": 26956274441, "runner_name": "GitHub Actions 1000486390", "started_at": "2026-06-04T16:22:09Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:52Z", "conclusion": "success", "created_at": "2026-06-04T13:55:14Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534224431, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273783", "run_id": 26956273783, "runner_name": "GitHub Actions 1000486244", "started_at": "2026-06-04T13:58:43Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:51Z", "conclusion": "success", "created_at": "2026-06-04T13:55:14Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534222287, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486225", "started_at": "2026-06-04T13:55:43Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:37Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348687, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486266", "started_at": "2026-06-04T14:04:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:10Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348760, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486254", "started_at": "2026-06-04T14:00:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:32:21Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348766, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486334", "started_at": "2026-06-04T14:22:48Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:19Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348788, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486327", "started_at": "2026-06-04T14:20:34Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:57:52Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348789, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486234", "started_at": "2026-06-04T13:56:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:04Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348811, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486268", "started_at": "2026-06-04T14:05:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:11Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348887, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486269", "started_at": "2026-06-04T14:05:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:22:17Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348888, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486328", "started_at": "2026-06-04T14:20:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:21:53Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348934, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486326", "started_at": "2026-06-04T14:20:24Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:49Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348956, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "azure-linux-scale-gtvf9-runner-24c5s", "started_at": "2026-06-04T13:57:26Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:33Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348960, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "azure-linux-scale-gtvf9-runner-dqpgd", "started_at": "2026-06-04T13:57:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:36:10Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348963, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "azure-linux-scale-gtvf9-runner-n5rfc", "started_at": "2026-06-04T13:58:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:15:04Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348975, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486271", "started_at": "2026-06-04T14:05:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:52Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348976, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486252", "started_at": "2026-06-04T14:00:41Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:36Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348988, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "azure-windows-scale-4lxzk-runner-h8ltr", "started_at": "2026-06-04T13:55:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:59:12Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534348995, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486239", "started_at": "2026-06-04T13:57:42Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:55Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349028, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486246", "started_at": "2026-06-04T13:59:22Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:59:57Z", "conclusion": "success", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349032, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486240", "started_at": "2026-06-04T13:57:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:25Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349044, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "azure-linux-scale-gtvf9-runner-dpw45", "started_at": "2026-06-04T13:57:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349332, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349448, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349722, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349823, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534349868, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534350131, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:52Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79534350143, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": null, "started_at": "2026-06-04T13:55:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Wire LowerBitcodeUKernels into the Mmt4dTilingExpert pipeline.\n\nCompletes the new-style C-ukernel pipeline integration:\n\n  * `LLVMCPUTarget.cpp`: install `iree_codegen.ukernel_provider =\n    #iree_cpu.ukernel_provider` on every LLVMCPU executable target\n    config whenever `--iree-llvmcpu-enable-llvm-ukernels` is non-empty.\n    This mirrors what ROCM does and is what lets the generic\n    `LowerBitcodeUKernelsPass` find the CPU provider for the\n    bitcode-lookup-and-attach.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)`: when\n    `selectUKernel` returns a descriptor, set the reduction-dim vector\n    tile size to 0 (untiled) instead of 1 (unrolled). The ukernel's C\n    function takes the outer K extent as a runtime argument\n    (`k_outer`) and runs the K loop itself; pre-unrolling here would\n    both defeat that and bloat the IR. Matches the equivalent split in\n    `setDataTiledMmaInnerTiledLoweringConfig` on the GPU side.\n\n  * `Passes.cpp::addMmt4dTilingExpertPassPipeline`: insert\n    `createLowerBitcodeUKernelsPass()` between the parallel-tile and\n    reduction-tile passes. A nop when no op carries a\n    `iree_codegen.ukernel` descriptor, so it stays in the default\n    pipeline. The companion `LowerUKernelOpsToCalls` already runs after\n    bufferization (in `addLowerToLLVMPasses`); together they implement\n    the user's \"early `inner_tiled` -> `ukernel.generic` on tensors;\n    late `ukernel.generic` -> `func.call` on memrefs\" split.\n\nLit test (`select_ukernel.mlir`) gains a second RUN line that chains\n`iree-llvmcpu-select-lowering-strategy` with\n`iree-codegen-lower-bitcode-ukernels` and asserts:\n  - the `inner_tiled` is rewritten to `iree_codegen.ukernel.generic`\n    carrying the right `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`\n    name;\n  - the matching bitcode appears as `hal.executable.objects` (resolved\n    from the global `EmbeddedDataDirectory` populated at LLVMCPU plugin\n    init);\n  - and the original `inner_tiled` is gone, guarding against the rewrite\n    silently no-op'ing on a regression.\n\nAn end-to-end `iree-compile` test would also be desirable, but the\ncurrent pipeline state needs more wiring (workgroup-distribution\nverifier complains about the bufferization-introduced copy ops around\nthe workgroup loop, on both the ukernel and non-ukernel paths) \u2014 that\ninvestigation belongs with the follow-up commit that fills in real\nukernel bodies and an actual numerical matmul test.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:36:18Z", "conclusion": "failure", "created_at": "2026-06-04T14:36:11Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-pipeline", "head_sha": "231e7fbda71c11a5ba6e5ca075ba8bbde6f34463", "job_id": 79542688122, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956273150", "run_id": 26956273150, "runner_name": "GitHub Actions 1000486369", "started_at": "2026-06-04T14:36:14Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:36Z", "conclusion": "success", "created_at": "2026-06-04T13:55:05Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79534191685, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956264641", "run_id": 26956264641, "runner_name": "GitHub Actions 1000486263", "started_at": "2026-06-04T14:03:32Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:45Z", "conclusion": "success", "created_at": "2026-06-04T13:55:05Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79534192268, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956264687", "run_id": 26956264687, "runner_name": "GitHub Actions 1000486226", "started_at": "2026-06-04T13:55:46Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "success", "created_at": "2026-06-04T13:55:05Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79534194453, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486281", "started_at": "2026-06-04T14:07:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:44Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869607, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486361", "started_at": "2026-06-04T14:27:35Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:33:11Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869624, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486366", "started_at": "2026-06-04T14:28:27Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:15Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869628, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486286", "started_at": "2026-06-04T14:08:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:12Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869715, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486285", "started_at": "2026-06-04T14:08:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:37:52Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869748, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486367", "started_at": "2026-06-04T14:28:28Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:22:20Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869754, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486295", "started_at": "2026-06-04T14:11:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:49Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869761, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486293", "started_at": "2026-06-04T14:10:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:11Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869771, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486283", "started_at": "2026-06-04T14:08:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:15Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869789, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486287", "started_at": "2026-06-04T14:08:44Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:32Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869798, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486284", "started_at": "2026-06-04T14:08:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:52Z", "conclusion": "failure", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869809, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "azure-linux-scale-gtvf9-runner-jlxbh", "started_at": "2026-06-04T14:08:08Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:54Z", "conclusion": "failure", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869817, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "azure-windows-scale-4lxzk-runner-z8qh6", "started_at": "2026-06-04T14:08:08Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:52Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869867, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486289", "started_at": "2026-06-04T14:10:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:16Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869882, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486330", "started_at": "2026-06-04T14:21:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:57Z", "conclusion": "failure", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869900, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "azure-linux-scale-gtvf9-runner-b9f92", "started_at": "2026-06-04T14:08:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:29:46Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869924, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486365", "started_at": "2026-06-04T14:28:20Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:16:12Z", "conclusion": "failure", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536869978, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "azure-linux-scale-gtvf9-runner-b6wqr", "started_at": "2026-06-04T14:08:21Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:51:52Z", "conclusion": "failure", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870072, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "azure-linux-scale-gtvf9-runner-8t7gh", "started_at": "2026-06-04T14:08:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870273, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:23:44Z", "conclusion": "success", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870322, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486297", "started_at": "2026-06-04T14:11:38Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870401, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870512, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870740, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870870, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870907, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:07Z", "conclusion": "skipped", "created_at": "2026-06-04T14:08:07Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79536870930, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": null, "started_at": "2026-06-04T14:08:07Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:51:59Z", "conclusion": "failure", "created_at": "2026-06-04T14:51:52Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79545930481, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265103", "run_id": 26956265103, "runner_name": "GitHub Actions 1000486372", "started_at": "2026-06-04T14:51:55Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:26Z", "conclusion": "success", "created_at": "2026-06-04T13:55:05Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79534194157, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486235", "started_at": "2026-06-04T13:56:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:54Z", "conclusion": "success", "created_at": "2026-06-04T13:56:26Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79534464794, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "azure-linux-scale-gtvf9-runner-ghst2", "started_at": "2026-06-04T13:58:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:18:39Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033795, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486316", "started_at": "2026-06-04T14:16:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:56Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033833, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-lhrbh", "started_at": "2026-06-04T14:11:03Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:35Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033890, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-p2vxj", "started_at": "2026-06-04T14:08:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:09Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033923, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486312", "started_at": "2026-06-04T14:15:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:57:05Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033927, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:52:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:19:22Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033932, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:15:51Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:51Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033936, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486296", "started_at": "2026-06-04T14:11:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:47:20Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033982, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:42:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:21:23Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033984, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-8rpx7", "started_at": "2026-06-04T14:13:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:26:18Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537033993, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486338", "started_at": "2026-06-04T14:23:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:29:15Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034004, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-kcn2x", "started_at": "2026-06-04T14:19:30Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:30Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034021, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486342", "started_at": "2026-06-04T14:24:11Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:59:49Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034026, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:51:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:40:51Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034030, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:34:27Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:05:41Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034050, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:55:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:12Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034052, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486336", "started_at": "2026-06-04T14:23:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:15Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034073, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486288", "started_at": "2026-06-04T14:08:58Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:43:02Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034113, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:36:29Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:16:55Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034118, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:14:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:55Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034147, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:10:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:32:03Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034151, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:27:39Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:28:58Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034164, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:22:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:40:19Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034177, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:34:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:55:32Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034193, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:49:50Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:17:06Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034217, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486303", "started_at": "2026-06-04T14:12:44Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:24Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034243, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:14:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:14:10Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034247, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:10:32Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:22:59Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034278, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486320", "started_at": "2026-06-04T14:18:42Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:08:25Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034279, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:01:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:02Z", "conclusion": "success", "created_at": "2026-06-04T14:08:55Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79537034334, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-95v9g", "started_at": "2026-06-04T14:25:38Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] SelectUKernels for inner_tiled + --iree-llvmcpu-enable-llvm-ukernels.\n\nWires the new C-bitcode ukernel framework into the LLVMCPU kernel-config\npipeline. Three pieces:\n\n  * `LLVMCPUSelectUKernels.{h,cpp}` \u2014 when the target config has\n    `llvm_ukernels = \"inner_tiled\"`, `selectUKernel` matches\n    `iree_codegen.inner_tiled` ops carrying a\n    `#iree_cpu.data_tiled_mma_layout` and returns a\n    `UKernelDescriptorAttr` whose name is the corresponding\n    `MMAIntrinsic` enum value lowercased with the `iree_uk_mma_` prefix\n    (e.g. `MMA_X86_AVX512BF16_1x16x2_F32_BF16` \u2192\n    `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16`). One ukernel per\n    intrinsic; `intrinsics_{m,n,k}` flow as function arguments and\n    specialize via bitcode LTO at the call site. Filters out `None` and\n    the `MMA_GENERIC_*` family \u2014 neither has a corresponding ukernel.\n\n  * `--iree-llvmcpu-enable-llvm-ukernels=<csv>` CL flag \u2014 bound to a new\n    `LLVMTarget.llvmUkernels` field, serialized into the target config as\n    `llvm_ukernels = \"<csv>\"`. Empty (the default) means off; passing\n    `inner_tiled` opts dispatches into the new framework. The flag name\n    leaves room for a future `mlir_ukernels`-style additional category\n    (and for now `inner_tiled` is also the only value). New helper\n    `hasLlvmUkernel(config, category)` in Codegen/Utils \u2014 distinct from\n    the existing `hasUkernel` to keep the legacy mmt4d ukernels and the\n    new LLVM-bitcode ukernels on independent enable flags.\n\n  * `KernelDispatch.cpp::setRootConfig(InnerTiledOp)` \u2014 calls\n    `selectUKernel` at the start and, on a match, sets the descriptor\n    via `setUKernelDescriptor`. Tile sizes and translation_info are\n    unchanged: the ukernel substitutes for the *innermost* intrinsic\n    execution only, the surrounding tile sizes and pipeline still hold.\n    Bitcode resolution is deferred to the `#iree_cpu.ukernel_provider`\n    during `LowerBitcodeUKernelsPass`, matching the GPU split between\n    \"match\" (here) and \"attach\" (provider).\n\nLit test (`select_ukernel.mlir`) runs\n`iree-llvmcpu-select-lowering-strategy` on two `inner_tiled` ops sharing\nthe same MMA intrinsic but differing in target config: the one with\n`llvm_ukernels = \"inner_tiled\"` picks up the\n`iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16` descriptor; the other does\nnot, locking in the \"off by default\" property.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:29:03Z", "conclusion": "success", "created_at": "2026-06-04T16:28:59Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-select", "head_sha": "5778accefe1ff991e133ab420a9c06c68be935b2", "job_id": 79565690515, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956265070", "run_id": 26956265070, "runner_name": "GitHub Actions 1000486391", "started_at": "2026-06-04T16:29:01Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:00:38Z", "conclusion": "success", "created_at": "2026-06-04T13:54:47Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534134081, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956246191", "run_id": 26956246191, "runner_name": "GitHub Actions 1000486222", "started_at": "2026-06-04T13:55:10Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:08Z", "conclusion": "success", "created_at": "2026-06-04T13:54:45Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534126571, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486221", "started_at": "2026-06-04T13:54:59Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:58:17Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206495, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486227", "started_at": "2026-06-04T13:55:47Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:01:04Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206513, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486232", "started_at": "2026-06-04T13:56:08Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:15Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206522, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486267", "started_at": "2026-06-04T14:04:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:17Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206525, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486223", "started_at": "2026-06-04T13:55:25Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:42Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206526, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486251", "started_at": "2026-06-04T14:00:32Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:05Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206528, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486265", "started_at": "2026-06-04T14:03:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:23Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206556, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486261", "started_at": "2026-06-04T14:02:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:16Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206566, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486260", "started_at": "2026-06-04T14:02:57Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:57:41Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206573, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486228", "started_at": "2026-06-04T13:55:49Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:58:13Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206583, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486233", "started_at": "2026-06-04T13:56:12Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:47Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206593, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486255", "started_at": "2026-06-04T14:01:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:16:27Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206608, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486270", "started_at": "2026-06-04T14:05:36Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:34Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206659, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486276", "started_at": "2026-06-04T14:07:06Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:29Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206661, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486273", "started_at": "2026-06-04T14:06:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:12Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206733, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "azure-linux-scale-gtvf9-runner-bcwvn", "started_at": "2026-06-04T13:56:56Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:30:00Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206742, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "azure-linux-scale-gtvf9-runner-c6cd7", "started_at": "2026-06-04T13:57:02Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:16Z", "conclusion": "failure", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206835, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "azure-windows-scale-4lxzk-runner-tqkd8", "started_at": "2026-06-04T13:55:10Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:03Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206967, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "azure-linux-scale-gtvf9-runner-9r25x", "started_at": "2026-06-04T13:56:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:02Z", "conclusion": "success", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534206976, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "azure-linux-scale-gtvf9-runner-dkdnf", "started_at": "2026-06-04T13:57:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207249, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207383, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207458, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207574, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207585, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207687, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:09Z", "conclusion": "skipped", "created_at": "2026-06-04T13:55:09Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534207834, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": null, "started_at": "2026-06-04T13:55:09Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:30:09Z", "conclusion": "failure", "created_at": "2026-06-04T14:30:01Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79541417989, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245614", "run_id": 26956245614, "runner_name": "GitHub Actions 1000486368", "started_at": "2026-06-04T14:30:04Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:57:00Z", "conclusion": "success", "created_at": "2026-06-04T13:54:45Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534126507, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245810", "run_id": 26956245810, "runner_name": "GitHub Actions 1000486220", "started_at": "2026-06-04T13:54:47Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:57Z", "conclusion": "success", "created_at": "2026-06-04T13:54:45Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534125268, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486219", "started_at": "2026-06-04T13:54:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:51Z", "conclusion": "success", "created_at": "2026-06-04T13:54:57Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79534166278, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "azure-linux-scale-gtvf9-runner-r6nrx", "started_at": "2026-06-04T13:55:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:10Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196706, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486299", "started_at": "2026-06-04T14:11:45Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:06Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196729, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-nr56p", "started_at": "2026-06-04T14:04:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:34Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196730, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:20:17Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:56Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196737, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-l68ql", "started_at": "2026-06-04T14:04:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:02:55Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196740, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:55:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:15:18Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196742, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486274", "started_at": "2026-06-04T14:06:41Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:25:14Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196748, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486344", "started_at": "2026-06-04T14:24:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:01:31Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196749, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:58:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:43:30Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196762, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:38:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:16:08Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196767, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486305", "started_at": "2026-06-04T14:12:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:15:50Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196776, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:14:18Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:28:57Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196777, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486306", "started_at": "2026-06-04T14:13:24Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:25:30Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196792, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:22:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:01Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196801, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:13:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:55:18Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196811, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:49:21Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:02:28Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196813, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:58:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:22Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196838, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486290", "started_at": "2026-06-04T14:10:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:55:01Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196839, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:51:23Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:57Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196855, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486318", "started_at": "2026-06-04T14:17:53Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:46Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196863, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-b29mc", "started_at": "2026-06-04T14:12:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:57Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196866, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-tggfr", "started_at": "2026-06-04T14:05:58Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:36:27Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196880, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:26:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:47:26Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196920, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:38:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:26:18Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196932, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486331", "started_at": "2026-06-04T14:21:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:26:55Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536196990, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:20:57Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:23Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536197048, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486314", "started_at": "2026-06-04T14:15:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:02:24Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536197097, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:55:33Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:38:14Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536197174, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:28:36Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T15:29:19Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536197338, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "shark55-ci", "started_at": "2026-06-04T15:25:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T14:16:32Z", "conclusion": "success", "created_at": "2026-06-04T14:04:51Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79536197382, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-sp6kk", "started_at": "2026-06-04T14:07:40Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Register embedded ukernel bitcode + provider lookup.\n\nWires the LLVMCPU plugin and the `#iree_cpu.ukernel_provider` attribute\nto actually resolve ukernel bitcode:\n\n  * On plugin init, the embedded `iree_uk_cpu_bitcode` TOC is iterated and\n    each `.bc` is added to the global `EmbeddedDataDirectory` (mirrors\n    `addAMDGPUUkernelBitcodeToGlobalEmbeddedDataDirectory` in\n    `ROCMTarget.cpp`).\n\n  * `UKernelProviderAttr::createAndReplaceWithUkernelOp` now resolves the\n    bitcode by name: user-supplied `hal.executable.objects` ancestors\n    first (BYO and BYO-override paths), then the embedded directory. If\n    found and not already on the op, the attribute is attached to the\n    source op; the default `LowerBitcodeUKernelsPass` rewrite then\n    preserves it on the resulting `ukernel.generic`. The provider still\n    returns `std::nullopt` for the rewrite itself \u2014 specialized\n    `inner_tiled` handling (scalar `intrinsics_{m,n,k}` + outer K count +\n    ACC inner stride) lands with SelectUKernels in a follow-up.\n\n  * Filename match is `<ukernelName>.<features>.bc` (or bare\n    `<ukernelName>.bc`), matching the build rules under\n    `compiler/plugins/target/LLVMCPU/builtins/ukernel/`. Multi-variant\n    disambiguation by target feature set also lands with SelectUKernels.\n\nBUILD.bazel: LLVMCPU plugin gains a dep on\n`//compiler/plugins/target/LLVMCPU/builtins/ukernel:iree_uk_cpu_bitcode`,\nand the `iree_cpu` dialect picks up HAL IR + `EmbeddedDataDirectory` so\nthe provider implementation can live with the attribute (consistent with\nhow `ROCMUkernelBitcodeSupport.cpp` sits inside the ROCM dialect).\n\nTests: a three-way lit test (`lookup_builtin_and_byo.mlir`) covers each\nbranch of the lookup:\n  1. Built-in only \u2014 no user objects; expect a `dense_resource<\u2026>` with\n     the embedded bf16 bitcode (2732 bytes) attached.\n  2. BYO override \u2014 user supplies bitcode under the built-in's filename;\n     expect the user's bytes to flow through unchanged (and CHECK-NOT\n     against `dense_resource` to catch a regression where the provider\n     silently swaps in the embedded copy).\n  3. Pure BYO \u2014 user-only name with no matching built-in; expect the\n     user's bitcode to surface on the rewritten op.\n\nCo-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>", "completed_at": "2026-06-04T16:15:54Z", "conclusion": "success", "created_at": "2026-06-04T16:15:50Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-bitcode-provider", "head_sha": "f0a0048b161154fb4a5d127e88c52f3f01f99f53", "job_id": 79563212938, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956245057", "run_id": 26956245057, "runner_name": "GitHub Actions 1000486389", "started_at": "2026-06-04T16:15:52Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "success", "created_at": "2026-06-04T13:54:01Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79533982004, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486204", "started_at": "2026-06-04T13:54:03Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:22Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018460, "labels": ["ubuntu-24.04"], "name": "runtime_wasm :: wasm32", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486210", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:01Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018517, "labels": ["windows-2022"], "name": "runtime :: windows-2022", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486215", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:59:17Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018526, "labels": ["ubuntu-24.04"], "name": "runtime_small", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486205", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:39Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018539, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486207", "started_at": "2026-06-04T13:54:16Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:45Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018595, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486216", "started_at": "2026-06-04T13:54:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:06Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018602, "labels": ["macos-14"], "name": "runtime :: macos-14", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486211", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:40Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018623, "labels": ["ubuntu-24.04-arm"], "name": "runtime_tracing :: ubuntu-24.04-arm :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486206", "started_at": "2026-06-04T13:54:17Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:02:31Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018638, "labels": ["azure-linux-scale"], "name": "linux_x64_clang / linux_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "azure-linux-scale-gtvf9-runner-h2c5w", "started_at": "2026-06-04T13:54:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:10Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018640, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486208", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:44Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018661, "labels": ["macos-14"], "name": "runtime_tracing :: macos-14 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486218", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:14Z", "conclusion": "success", "created_at": "2026-06-04T13:54:12Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018670, "labels": ["ubuntu-24.04-arm"], "name": "runtime :: ubuntu-24.04-arm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486217", "started_at": "2026-06-04T13:54:18Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:34Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018727, "labels": ["windows-2022"], "name": "runtime_tracing :: windows-2022 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486212", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:52Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018735, "labels": ["ubuntu-24.04"], "name": "runtime :: ubuntu-24.04", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486213", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:47Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018758, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: tracy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486209", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:42Z", "conclusion": "failure", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018767, "labels": ["azure-windows-scale"], "name": "windows_x64_msvc / windows_x64_msvc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "azure-windows-scale-4lxzk-runner-w9v8d", "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:19Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018847, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_asan / linux_x64_clang_asan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "azure-linux-scale-gtvf9-runner-ksb7k", "started_at": "2026-06-04T13:54:52Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:39:08Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018869, "labels": ["azure-linux-scale"], "name": "linux_x64_bazel / linux_x64_bazel", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "azure-linux-scale-gtvf9-runner-cnvp7", "started_at": "2026-06-04T13:54:30Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:31Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534018870, "labels": ["azure-linux-scale"], "name": "linux_x64_clang_ubsan / linux_x64_clang_ubsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "azure-linux-scale-gtvf9-runner-fpxbk", "started_at": "2026-06-04T13:54:58Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:55:41Z", "conclusion": "success", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019107, "labels": ["ubuntu-24.04"], "name": "runtime_tracing :: ubuntu-24.04 :: console", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486214", "started_at": "2026-06-04T13:54:15Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019239, "labels": [], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019333, "labels": [], "name": "linux_x64_clang_debug", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019357, "labels": [], "name": "macos_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019424, "labels": [], "name": "linux_arm64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019608, "labels": [], "name": "linux_x64_clang_tsan", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019620, "labels": [], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:12Z", "conclusion": "skipped", "created_at": "2026-06-04T13:54:13Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534019824, "labels": [], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": null, "started_at": "2026-06-04T13:54:13Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:39:14Z", "conclusion": "failure", "created_at": "2026-06-04T14:39:08Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79543301909, "labels": ["ubuntu-24.04"], "name": "ci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203814", "run_id": 26956203814, "runner_name": "GitHub Actions 1000486371", "started_at": "2026-06-04T14:39:11Z", "status": "completed", "workflow_id": 28673, "workflow_name": "CI", "workflow_path": ".github/workflows/ci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:54:11Z", "conclusion": "success", "created_at": "2026-06-04T13:54:00Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79533978324, "labels": ["ubuntu-24.04"], "name": "setup / setup", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486203", "started_at": "2026-06-04T13:54:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:03:03Z", "conclusion": "success", "created_at": "2026-06-04T13:54:11Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79534014183, "labels": ["azure-linux-scale"], "name": "Build Packages / Linux Release (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "azure-linux-scale-gtvf9-runner-l86z2", "started_at": "2026-06-04T13:54:12Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:06:39Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830372, "labels": ["linux-mi35x-1gpu-ossci-iree-org"], "name": "Test AMD MI355 / test_mi355", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "linux-mi35x-1gpu-ossci-iree-org-ll5cm-runner-zqz8k", "started_at": "2026-06-04T14:03:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:15Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830440, "labels": ["Linux", "X64", "iree-r9700"], "name": "Test AMD R9700 / test_r9700", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:03:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:27:10Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830499, "labels": ["ubuntu-24.04"], "name": "Test RISC-V 64 / riscv64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486298", "started_at": "2026-06-04T14:11:43Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:17:51Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830522, "labels": ["ubuntu-24.04"], "name": "Unit Test / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486313", "started_at": "2026-06-04T14:15:20Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:08Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830585, "labels": ["ubuntu-24.04"], "name": "Test Android / android_arm64", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486264", "started_at": "2026-06-04T14:03:46Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:13:32Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830601, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cuda)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486294", "started_at": "2026-06-04T14:10:34Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:07:45Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830614, "labels": ["Linux", "X64", "iree-w7900x2", "persistent-cache"], "name": "Test Sharktank / sharktank_model_tests :: rocm_hip_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:03:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:14:31Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830624, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test Sharktank / sharktank_tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:03:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:55Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830630, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / torch_models tests :: amdgpu_mi325_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-fpjn7", "started_at": "2026-06-04T14:03:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:58:51Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830642, "labels": ["Linux", "X64", "iree-w7900"], "name": "Test AMD W7900 / test_w7900", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:55:02Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:19:40Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830647, "labels": ["ubuntu-24.04"], "name": "Test PJRT plugin / Build and test (ubuntu-24.04, cpu)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486315", "started_at": "2026-06-04T14:16:10Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:12:44Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830671, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi300_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-4vbhp", "started_at": "2026-06-04T14:03:05Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:11:39Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830683, "labels": ["Linux", "X64", "rdna3"], "name": "Test ONNX / test_onnx_ops :: amdgpu_vulkan_rdna3_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:03:22Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:43Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830706, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O0", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486280", "started_at": "2026-06-04T14:07:31Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:24:20Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830710, "labels": ["Linux", "X64", "gfx1201"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna4_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark75-ci", "started_at": "2026-06-04T14:11:16Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:05:49Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830712, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test AMD MI325 / test_mi325", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-mvkpt", "started_at": "2026-06-04T14:03:47Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:11:41Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830714, "labels": ["ubuntu-24.04"], "name": "Test ONNX / test_onnx_ops :: cpu_llvm_sync_O2", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486279", "started_at": "2026-06-04T14:07:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:00:06Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830746, "labels": ["Linux", "X64", "rdna3"], "name": "Test Torch / test_torch_ops :: amdgpu_vulkan_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark01-ci", "started_at": "2026-06-04T14:57:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:15:30Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830770, "labels": ["Linux", "X64", "gfx1201"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1201_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark75-ci", "started_at": "2026-06-04T15:12:15Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:49:19Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830771, "labels": ["self-hosted", "persistent-cache", "Linux", "X64"], "name": "Test ONNX / test_onnx_models :: cpu_llvm_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:43:04Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:10:04Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830777, "labels": ["Linux", "X64", "gfx1100"], "name": "Test Torch / test_torch_ops :: amdgpu_hip_gfx1100_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark55-ci", "started_at": "2026-06-04T14:03:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:14:16Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830779, "labels": ["Linux", "X64", "gfx1201", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna4", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark75-ci", "started_at": "2026-06-04T16:11:28Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:08:56Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830803, "labels": ["ubuntu-24.04"], "name": "Test Torch / test_torch_ops :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486282", "started_at": "2026-06-04T14:08:08Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:27:38Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830817, "labels": ["self-hosted", "persistent-cache", "Linux", "X64", "threadripper"], "name": "Test Torch / torch_models tests :: cpu_task", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:24:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:13:33Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830819, "labels": ["nodai-amdgpu-mi308-x86-64"], "name": "Test Sharktank / sharktank_tests :: amdgpu_rocm_mi308_gfx942", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "iree-mi308-1", "started_at": "2026-06-04T14:03:06Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:04:15Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830833, "labels": ["linux-mi325-1gpu-ossci-iree-org"], "name": "Test Torch / test_torch_ops :: amdgpu_rocm_mi300_gfx942_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "linux-mi325-1gpu-ossci-iree-org-2gghx-runner-zg7l2", "started_at": "2026-06-04T14:03:14Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:10:31Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830835, "labels": ["Linux", "X64", "gfx1100", "persistent-cache"], "name": "Test ONNX / test_onnx_models :: amdgpu_hip_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark10-ci", "started_at": "2026-06-04T15:06:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:20:16Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830857, "labels": ["Linux", "X64", "rdna3", "persistent-cache", "shark10-ci"], "name": "Test ONNX / test_onnx_models :: amdgpu_vulkan_rdna3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark10-ci", "started_at": "2026-06-04T14:14:59Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T15:24:59Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830893, "labels": ["Linux", "X64", "gfx1100"], "name": "Test ONNX / test_onnx_ops :: amdgpu_hip_rdna3_O3", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "shark01-ci", "started_at": "2026-06-04T15:14:55Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T14:13:23Z", "conclusion": "success", "created_at": "2026-06-04T14:03:04Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79535830939, "labels": ["ubuntu-24.04"], "name": "Test TensorFlow / Linux (x86_64)", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486300", "started_at": "2026-06-04T14:11:54Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T16:14:21Z", "conclusion": "success", "created_at": "2026-06-04T16:14:17Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79562907160, "labels": ["ubuntu-24.04"], "name": "pkgci_summary / summary", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956203301", "run_id": 26956203301, "runner_name": "GitHub Actions 1000486388", "started_at": "2026-06-04T16:14:19Z", "status": "completed", "workflow_id": 67146182, "workflow_name": "PkgCI", "workflow_path": ".github/workflows/pkgci.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:56:22Z", "conclusion": "success", "created_at": "2026-06-04T13:54:00Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79533977836, "labels": ["ubuntu-24.04"], "name": "pre-commit", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956202633", "run_id": 26956202633, "runner_name": "GitHub Actions 1000486202", "started_at": "2026-06-04T13:54:04Z", "status": "completed", "workflow_id": 1690000, "workflow_name": "Lint", "workflow_path": ".github/workflows/lint.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Codegen][CPU] Seed C-bitcode ukernel framework with bf16 and i8 seeds.\n\nAdds compiler/plugins/target/LLVMCPU/builtins/ukernel/, the new home for\nLLVMCPU-only C microkernels. Modelled after the ROCM C ukernel framework\n(compiler/plugins/target/ROCM/builtins/ukernel/): compile each ukernel to\nLLVM bitcode, embed those .bc files into iree-compile, and emit them as\nself-contained `hal.executable_object`s on dispatch variants \u2014 distinct\nfrom the legacy mmt4d ukernels under runtime/, which serve a now-defunct\nVMVX path and are built twice.\n\nThis first commit lands the scaffolding plus two inner-K-loop seeds with\nstub bodies (filled in by a later commit):\n  * `iree_uk_mma_x86_avx512bf16_1x16x2_f32_bf16.c` (bf16 x bf16 -> f32)\n    and `iree_uk_mma_x86_avx512vnni_16x16x2_i32_i8_casti16.c`\n    (i8 x i8 -> i32 via VNNI), plus `common.h` (no-stdlib stdint\n    replacements);\n  * `iree_bitcode_library` + `iree_c_embed_data` rules, driven by a\n    `(stem, features, copts)` table so adding a seed is one row (CMake\n    regenerated; `iree_bitcode_library` in `bazel_to_cmake_converter.py`\n    extended to forward `out`, so the embedded filename is\n    `<name>.<arch>_<features>.bc`);\n  * a `README.md` design doc covering scope, build, IR representation,\n    end-to-end flow, and authoring/test guidance;\n  * `#iree_cpu.ukernel_provider`, the CPU analogue of\n    `#rocm.ukernel_provider`, declared in `IREECPUAttrs.td`/`.cpp` with a\n    `createAndReplaceWithUkernelOp` that currently delegates to the\n    default `LowerBitcodeUKernelsPass` fallback. Specialized\n    `inner_tiled` handling (threading `intrinsics_{m,n,k}` and the outer\n    K count) will land in a follow-up alongside `SelectUKernels` and\n    pipeline integration.\n\nTests:\n  * roundtrip lit test for the new `#iree_cpu.ukernel_provider`\n    attribute;\n  * lit tests running `--iree-codegen-lower-bitcode-ukernels` on ops\n    carrying both `iree_codegen.ukernel = \"...\"` and a user-supplied\n    `hal.executable.objects` bitcode (one per seed), asserting they are\n    rewritten to `iree_codegen.ukernel.generic` with the bitcode\n    preserved.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>", "completed_at": "2026-06-04T13:58:41Z", "conclusion": "success", "created_at": "2026-06-04T13:53:59Z", "event": "pull_request", "head_branch": "users/bjacob/cpu-ukernel-framework-seed", "head_sha": "439bb8a9d2d5ae0011626a775a903f8355c06624", "job_id": 79533976028, "labels": ["ubuntu-24.04"], "name": "clang-tidy", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26956201999", "run_id": 26956201999, "runner_name": "GitHub Actions 1000486201", "started_at": "2026-06-04T13:54:02Z", "status": "completed", "workflow_id": 225853312, "workflow_name": "Clang Tidy", "workflow_path": ".github/workflows/clang_tidy.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T13:39:13Z", "conclusion": "failure", "created_at": "2026-06-04T10:28:13Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499538990, "labels": ["macos-15-intel"], "name": "macos_x64_clang", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946159967", "run_id": 26946159967, "runner_name": "GitHub Actions 1000486179", "started_at": "2026-06-04T10:28:15Z", "status": "completed", "workflow_id": 109041301, "workflow_name": "CI - macOS x64 clang", "workflow_path": ".github/workflows/ci_macos_x64_clang.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T15:31:28Z", "conclusion": "success", "created_at": "2026-06-04T10:28:29Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499580716, "labels": ["ubuntu-24.04"], "name": "linux_x64_clang_byollvm", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946172053", "run_id": 26946172053, "runner_name": "GitHub Actions 1000486181", "started_at": "2026-06-04T10:28:30Z", "status": "completed", "workflow_id": 109041212, "workflow_name": "CI - Linux x64 clang BYO LLVM", "workflow_path": ".github/workflows/ci_linux_x64_clang_byollvm.yml"}
{"collected_at": "2026-06-04T19:06:24.687262Z", "commit_message": "[Runtime][Python] Add pyright checks and fix errors (#24561)\n\nSigned-off-by: Florian Walbroel <walbroel@roofline.ai>", "completed_at": "2026-06-04T14:39:36Z", "conclusion": "success", "created_at": "2026-06-04T10:28:39Z", "event": "schedule", "head_branch": "main", "head_sha": "4bced6946652ec77a49919b0b0a2ffad93a87b39", "job_id": 79499604983, "labels": ["ubuntu-24.04"], "name": "linux_x64_gcc", "run_attempt": 1, "run_html_url": "https://github.com/iree-org/iree/actions/runs/26946179588", "run_id": 26946179588, "runner_name": "GitHub Actions 1000486182", "started_at": "2026-06-04T10:28:40Z", "status": "completed", "workflow_id": 109041211, "workflow_name": "CI - Linux x64 gcc", "workflow_path": ".github/workflows/ci_linux_x64_gcc.yml"}
